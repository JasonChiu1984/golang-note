# 內容需要更新的部分

> 產出日期：2026-07-10
> 完整日期時間：2026-07-10 06:02:42 CST +0800
> 依據審查報告：`審查報告/2026-07-10-060242-資深工程師審查報告.md`
> 目標版本：v1.0.84
> 主題：Release publish reconciliation evidence

## 必須更新

1. 新增 release publish reconciliation contract gate
   - 新增 `scripts/check-release-publish-reconciliation-contract.mjs`。
   - 檢查 `更新資料/2026-07-09-060302-v1.0.83-更新紀錄.md` 必須保留 `remote v1.0.83 created / final release-record amend local-only`、`force-with-lease`、`HEAD`、`origin/main`、`v1.0.83^{}` 與 recovery command。
   - 檢查本輪教材文件必須提到 `Release publish reconciliation contract gate` 與 `node scripts/check-release-publish-reconciliation-contract.mjs`。

2. 更新 contract gate inventory
   - root `scripts/check-*-contract.mjs` inventory 從 49 個更新為 50 個。
   - 更新 `scripts/check-contract-gate-inventory-contract.mjs` 的實際比較條件與訊息。
   - 更新 CI 呼叫清單與 `production-api-worker/Makefile`。

3. 更新教材與專案文件
   - `README.md`：版本改為 `v1.0.84`，新增本輪 release publish reconciliation 摘要。
   - `CHANGELOG.md`：新增 `v1.0.84 - 2026-07-10`。
   - `production-api-worker/README.md`：新增 release publish reconciliation gate 說明與 Make target。
   - `production-api-worker/docs/api-contract.md`：更新版本、日期與 scope，加入 publish reconciliation evidence。
   - `production-api-worker/api/openapi.yaml`：更新 version，加入 release publish reconciliation governance summary。
   - `production-api-worker/docs/operational-runbook.md`：更新日期、完整日期時間、版本與適用範圍。
   - `chapters/07-large-project-concurrent-crawler.md`：contract gate inventory 表格新增 release publish reconciliation row。
   - `chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md`：加入 release publish reconciliation gate。
   - `圖解筆記3-4整合/golang-complete-visual-course.html`：版本與 Production Debug / CI-CD 描述加入本輪主題。

4. 更新 version-sensitive checker
   - 將既有 version-sensitive contract checker 從 `v1.0.83` 更新為 `v1.0.84`。
   - 將 `scripts/check-release-artifact-chain-contract.mjs` 的 artifact timestamp 更新為 `2026-07-10-060242`，並要求本輪三份 artifact。

5. 同步 docs 首頁
   - 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製為 `docs/index.html`。
   - 執行 `node scripts/fix-docs-index-links.mjs --sync-source` 與 `--check`。

## 驗證與發版

- `node scripts/check-release-publish-reconciliation-contract.mjs`
- `cd production-api-worker && make release-publish-reconciliation-check`
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- 全 root `scripts/check-*-contract.mjs` sweep，預期 50 個通過。
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- Ruby YAML parse `.github/workflows/ci.yml` 與 `production-api-worker/api/openapi.yaml`
- `cd production-api-worker && docker compose config`
- `git diff --check`
- 最終 commit、annotated tag `v1.0.84`，並推送 `main` 與 tag 到 GitHub。
