# 內容需要更新的部分

| 項目 | 內容 |
|---|---|
| 產生日期 | 2026-05-13 |
| 完整日期時間 | 2026-05-13 22:55:44 CST |
| 對應審查報告 | `審查報告/2026-05-13-225544-資深工程師審查報告.md` |
| 目標版本 | v1.0.28 |

## 必須更新

| 檔案 / 範圍 | 更新內容 | 驗收條件 |
|---|---|---|
| `production-api-worker/internal/config` | 新增 `API_KEY` 設定讀取與 trim 行為 | config test 固定 default / env 行為 |
| `production-api-worker/internal/api` | 新增 auth middleware、安全標頭 middleware、`401 unauthorized` envelope | `TestAPIKeyAuthContract`、`TestSecurityHeadersContract` 通過 |
| `production-api-worker/cmd/api-worker` | 將 `cfg.APIKey` 注入 handler | `go test ./internal/api` 通過 |
| `production-api-worker/scripts/compose-smoke.sh` | 若 `API_KEY` 有值則送 `Authorization: Bearer` | `bash -n` 通過，啟用 auth 時 smoke script 可沿用 |
| `production-api-worker/docker-compose.yml` | 支援由環境注入 `API_KEY` | Compose 可用 `${API_KEY:-}` |
| `README.md` | 新增 API security contract 版本策略與驗證指令 | README 有 `API_KEY`、`TestAPIKeyAuthContract` |
| `production-api-worker/README.md` | 補上 API security contract、curl/Compose 範例與 release gate | 文件可直接指導 local / Docker 驗證 |
| `production-api-worker/docs/api-contract.md` | 升版至 v1.0.28，補上 auth、security headers、`unauthorized` code | 合約文件與測試名稱一致 |
| `chapters/07-large-project-concurrent-crawler.md` | 補上 public probe vs protected endpoint 的 production 邊界 | 第 7 章能說明為何 health 不擋、metrics 要擋 |
| `chapters/11-advanced-testing.md` | 補上 API security contract test 教學 | 第 11 章含測試範例與驗證指令 |
| `Cheatsheet/cheatsheet-advanced.md` | 補上 API security contract 速查 | 速查表可作維運 checklist |
| `圖解筆記3-4整合/golang-complete-visual-course.html` | 補上 security contract 關鍵字與剩餘進階安全方向 | `docs/index.html` 同步後可在 Pages 顯示 |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| OAuth2 / OIDC / mTLS | 超出本輪最小安全合約，需另開完整 identity / gateway 教材 |
| Rate limit | 需要 token bucket、IP / tenant key、metrics 與 load test，建議下一輪處理 |
| Secret rotation | 需要配置來源與部署流程，例如 Kubernetes Secret、Vault 或 CI secret |
