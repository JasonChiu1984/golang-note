# 內容需要更新的部分

> 產生時間：2026-05-13 06:03:49 +0800  
> 對應審查報告：`審查報告/2026-05-13-060349-資深工程師審查報告.md`  
> 目標版本：v1.0.9

## 本輪更新主題

補齊 production API 合約、錯誤格式、相容性測試與 release gate，讓教程的專案開發深度不只停在「功能可執行」，也包含「對外合約可維護」。

## 更新清單

| 項目 | 檔案 | 更新內容 |
|---|---|---|
| 版本入口 | `README.md`、`VERSION`、`CHANGELOG.md` | 升級到 `v1.0.9`，新增 API contract 更新摘要 |
| 大型專案章 | `chapters/07-large-project-concurrent-crawler.md` | 新增 API 合約、錯誤 envelope、相容性規則與 contract test gate |
| 測試章 | `chapters/11-advanced-testing.md` | 新增 API contract test 範例與驗證矩陣 |
| Production 專案 | `production-api-worker/README.md` | 新增 API contract section 與 release quality gate |
| API 合約文件 | `production-api-worker/docs/api-contract.md` | 定義 endpoint、request/response schema、錯誤 code、health/metrics 與測試 gate |
| API 錯誤格式 | `production-api-worker/internal/api/handler.go` | 改成 `error.code` / `error.message` envelope |
| 合約測試 | `production-api-worker/internal/api/handler_test.go` | 新增成功與錯誤回應 contract tests |
| 輔助教材 | `Cheatsheet/cheatsheet-advanced.md`、`康乃爾筆記法/07-大型專案架構.md` | 同步補上 API 合約速查與康乃爾摘要 |

## 驗證要求

```bash
git diff --check
go test ./internal/api -run 'Test.*Contract' -count=1
go test ./internal/api -count=1
```

> `production-api-worker` 第一次完整 `go test ./...` 仍可能因離線環境無法下載 module 而失敗；本輪以已存在 module cache 可執行的 API contract tests 作為直接驗收。
