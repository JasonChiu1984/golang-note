# 2026-06-18 06:02:10 CST +0800 內容需要更新的部分

## 依據

參照 `審查報告/2026-06-18-060210-資深工程師審查報告.md`，本輪更新集中處理 release artifact chain 尚未被正式 static gate 固定的缺口。

## 需要更新

1. 新增 Release artifact chain contract checker。
   - 新增 `scripts/check-release-artifact-chain-contract.mjs`。
   - 檢查 `VERSION`、`CHANGELOG.md`、`審查報告/`、`內容需要更新的部分/`、`更新資料/`、`docs/index.html` 與整合課程來源。
   - 固定最新版本 `v1.0.62` 必須有同 timestamp 的三段 artifact，且更新紀錄需標示驗證、docs sync、commit/tag/push 狀態。

2. 接入 Makefile 與 GitHub Actions。
   - 在 `production-api-worker/Makefile` 新增 `release-artifact-chain-check`。
   - 在 `.github/workflows/ci.yml` 新增 `Check release artifact chain contract`。

3. 更新版本與文件入口。
   - `VERSION`、`README.md`、`CHANGELOG.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml` 更新為 `v1.0.62`。
   - README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程補上 Release artifact chain contract gate。

4. 更新 contract gate inventory。
   - root contract checker 數量由 30 個更新為 31 個。
   - 確認 `scripts/check-contract-gate-inventory-contract.mjs`、CI 與教材文件同步。

5. 發布同步與驗證。
   - 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`。
   - 執行 docs link checks、contract checks、Go tests、Docker Compose config 與 diff hygiene。
