# 2026-07-08 內容需要更新的部分

- 完整日期時間：2026-07-08 06:02:28 CST +0800
- 參照審查報告：2026-07-08-060228-資深工程師審查報告.md
- 目標版本：v1.0.82
- 更新主題：Go ReleaseNote patch baseline refresh

## 必要更新

1. 將 `VERSION`、`CHANGELOG.md`、README、API contract、OpenAPI、version-sensitive contract scripts 的版本標記從 `v1.0.81` 更新為 `v1.0.82`。
2. 將 Go ReleaseNote 基準從 `go1.26.4` / `go1.25.11` 更新為官方 2026-07-07 發布的 `go1.26.5` / `go1.25.12`。
3. 更新 `scripts/generate-go-release-notes.mjs` 的 support status、首頁 lead 文字與 Release History patch 擷取後的輸出。
4. 更新 `scripts/check-go-release-notes-contract.mjs` 的 expected patch tokens，讓 contract gate 固定 `go1.26.5` / `go1.25.12`。
5. 重新產生並同步 `ReleaseNote/` 與 `docs/ReleaseNote/`，確認 `go1.26-release-note.html` 與 `go1.25-release-note.html` 含最新 patch row。
6. 更新 `README.md` 與 `chapters/01-environment-and-project.md` 的 Go 1.26 安裝與測試命令為 `go1.26.5`。
7. 更新 `圖解筆記3-4整合/golang-complete-visual-course.html` 的 release gate / readiness wording，再將其複製同步至 `docs/index.html`。
8. 更新 `scripts/check-release-artifact-chain-contract.mjs`，使本輪 artifact trio 與 v1.0.82 發布主題可被 release artifact chain gate 驗證。

## 驗證需求

1. `node scripts/check-go-release-notes-contract.mjs`
2. `node scripts/check-release-artifact-chain-contract.mjs`
3. `node scripts/check-contract-gate-inventory-contract.mjs`
4. 全部 root `scripts/check-*-contract.mjs`
5. `node scripts/fix-docs-index-links.mjs --check`
6. `node scripts/check-html-home-links.mjs`
7. GitHub Actions / OpenAPI YAML parse
8. `cd production-api-worker && docker compose config`
9. `git diff --check`

## 不做事項

- 不新增第 50 個 root contract checker。
- 不修改 production-api-worker runtime behavior。
- 不把 Alertmanager routing governance 重新包成新 release 主題；本輪只處理官方 Go patch baseline drift。
