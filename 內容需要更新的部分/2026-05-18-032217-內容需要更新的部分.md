# 內容需要更新的部分

| 項目 | 內容 |
|---|---|
| 產生日期 | 2026-05-18 |
| 完整日期時間 | 2026-05-18 03:22:17 CST +0800 |
| 對應審查報告 | `審查報告/2026-05-18-032217-資深工程師審查報告.md` |
| 目標版本 | v1.0.35 |

## 必須更新

| 優先級 | 更新項目 | 目的 | 驗收方式 |
|---|---|---|---|
| P1 | API contract / OpenAPI version 改為 `v1.0.35` | 避免 machine-readable contract 與教材版本漂移 | `node scripts/check-openapi-contract.mjs` |
| P1 | rate limit contract check script 同步 OpenAPI version | 避免舊版字串卡住新版本 gate | `node scripts/check-rate-limit-contract.mjs` |
| P1 | trusted proxy client IP 邊界納入 rate limit contract | 避免未信任外部來源偽造 `X-Forwarded-For` 影響 per-client limit | `go test ./internal/api -run 'TestRateLimitTrustedProxyContract' -count=1` |
| P1 | `docs/index.html` 由 `golang-complete-visual-course.html` 重新同步 | 確保 GitHub Pages 首頁含 shutdown signal contract | `node scripts/fix-docs-index-links.mjs --check` |
| P1 | 補齊 `v1.0.35` 更新紀錄 | 讓本輪自動化流程可追溯 | 檢查 `更新資料/2026-05-18-032217-v1.0.35-更新紀錄.md` |
| P1 | commit、tag、push `v1.0.35` | 將版本號與修改內容標註在 GitHub | `git tag v1.0.35`、`git push origin main refs/tags/v1.0.35` |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| 新增 API feature | 本輪重點是版本收斂，不擴大行為面 |
| 重構 handler middleware | 現有 shutdown signal 缺口不需要改 handler |
| 實機 Kubernetes rollout 測試 | 本地可驗證範圍以 signal set、readiness、queue drain 文件與 CI gate 為主 |
