# 2026-07-16 內容需要更新的部分

- 產生日期：2026-07-16
- 完整日期時間：2026-07-16 06:02:04 CST +0800
- 依據審查報告：`審查報告/2026-07-16-060204-資深工程師審查報告.md`
- 目標版本：v1.0.90
- 本輪主題：Release artifact continuity index contract gate
- Contract gate inventory：54 個 root contract checker

## 更新項目

| 項目 | 需要更新的內容 | 驗收方式 |
|---|---|---|
| release artifact continuity checker | 新增 `scripts/check-release-artifact-continuity-contract.mjs`，檢查 `審查報告/`、`內容需要更新的部分/`、`更新資料/` 是否保留同版三件套、timestamp 順序、版本連續性與 update record 自我引用。 | `node scripts/check-release-artifact-continuity-contract.mjs` |
| Makefile / CI | 新增 `release-artifact-continuity-check` target，並加入 GitHub Actions root course job。 | `cd production-api-worker && make release-artifact-continuity-check`、CI YAML parse |
| contract gate inventory | root checker 數量由 53 推進到 54，所有 inventory-aware surfaces 必須同步。 | `node scripts/check-contract-gate-inventory-contract.mjs` |
| release version consistency | `VERSION`、`CHANGELOG.md`、README、API contract、OpenAPI、章節、Cheatsheet、整合視覺課程、`docs/index.html` 推進到 `v1.0.90`。 | `node scripts/check-release-version-consistency-contract.mjs` |
| release artifact chain / metadata | 本輪三件套使用 `2026-07-16-060204` timestamp，並保留完整日期時間、來源報告與本輪主題。 | `node scripts/check-release-artifact-chain-contract.mjs`、`node scripts/check-release-artifact-metadata-contract.mjs` |
| docs publishing | 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製到 `docs/index.html`，並修正 Pages link。 | `node scripts/fix-docs-index-links.mjs --check`、`node scripts/check-html-home-links.mjs` |

## 不在本輪更新的項目

- 不新增 production runtime API、DB schema、worker behavior 或 observability metric。
- 不更新 Go ReleaseNote patch baseline，因官方 Go Release History 查核結果仍可沿用 go1.26.5 / go1.25.12。
- 不調整 UI 設計風格，只補齊整合視覺課程內的 release gate 條目與版本文字。

## 完成定義

1. 新增 continuity checker 並納入 Makefile / CI。
2. 三份 release artifact、版本文字、docs/index 與 contract gate inventory 全部對齊。
3. root contract checker sweep、docs checks、YAML / Compose checks、Go tests 與 `git diff --check` 通過或明確記錄環境限制。
4. 建立 `v1.0.90` annotated tag，推送 `main` 與 tag 至 GitHub；若網路阻擋，需在 `更新資料` 與 automation memory 保留 local-only publish state。
