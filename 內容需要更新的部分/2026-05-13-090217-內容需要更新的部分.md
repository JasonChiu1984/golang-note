# 2026-05-13 09:02 內容需要更新的部分

> 來源審查報告：`審查報告/2026-05-13-090217-資深工程師審查報告.md`  
> 目標版本：`v1.0.12`  
> 更新主題：Production HTTP panic recovery 合約

## 必要更新

| 類別 | 檔案 | 更新內容 |
|---|---|---|
| 程式 | `production-api-worker/internal/api/handler.go` | 新增 recover middleware，確保 handler / service / queue panic 轉為 `500 internal_error` JSON |
| 測試 | `production-api-worker/internal/api/handler_test.go` | 新增 `TestPanicRecoveryContract`，驗證 status、JSON envelope、`X-Request-ID` |
| API 文件 | `production-api-worker/docs/api-contract.md` | 將 panic recovery 納入 compatibility rules、error envelope 與 contract gate |
| 主入口 | `README.md` | 升級到 `v1.0.12`，新增 panic recovery 版本策略與驗證指令 |
| Production 範例 | `production-api-worker/README.md` | 新增 release quality gate 與 panic recovery 說明 |
| 教程章節 | `chapters/07-large-project-concurrent-crawler.md` | 補上 recover middleware 的 production 邊界、middleware 順序與排障價值 |
| 測試章節 | `chapters/11-advanced-testing.md` | 補上 panic recovery contract test 的測試目標 |
| 速查 / 筆記 | `Cheatsheet/cheatsheet-advanced.md`、`康乃爾筆記法/07-大型專案架構.md` | 同步 release 前檢查與複習摘要 |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| 新增外部 observability / recovery 套件 | 本輪需求可用標準 `recover` 完成，不需要引入依賴 |
| DB schema 或 migration 變更 | panic recovery 屬於 HTTP middleware 邊界，不需要資料庫變更 |
| Go 版本基準調整 | 本輪未重新定義 Go 1.26.3 教材基準 |

## 預期完成狀態

- `production-api-worker` panic path 回 `500`。
- Response 維持 `{"error":{"code":"internal_error","message":"internal error"}}`。
- Client 提供的 `X-Request-ID` 在 panic path 仍原樣回傳。
- README / 章節 / Cheat Sheet / 康乃爾筆記都有對應說明。
- `CHANGELOG.md` 與 `VERSION` 標註 `v1.0.12`。

