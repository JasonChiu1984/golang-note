# 內容需要更新的部分

- 建立日期：2026-06-26
- 完整建立時間：2026-06-26 06:06:00 CST +0800
- 對應審查報告：`審查報告/2026-06-26-060600-資深工程師審查報告.md`
- 目標版本：v1.0.70
- 更新主題：Release rollback drill contract gate

## 需要修正的缺口

目前教材已具備 CI quality、production workflow、release artifact chain、dependency governance 與 performance benchmark governance，但 rollback drill 仍停留在分散式文字建議。若之後章節或 runbook 改版，無 static gate 可防止 rollback decision、previous image restore、migration rollback boundary、health verification、metrics verification 與 postmortem evidence 被移除。

## 更新項目

| 項目 | 更新內容 | 驗收方式 |
|---|---|---|
| Root checker | 新增 `scripts/check-release-rollback-drill-contract.mjs` | `node --check` 與直接執行通過 |
| Makefile | 新增 `release-rollback-drill-check` | `cd production-api-worker && make release-rollback-drill-check` 通過 |
| CI | 新增 `Check release rollback drill contract` | `.github/workflows/ci.yml` 包含 checker 呼叫 |
| README | 新增 Release rollback drill contract gate 與 39 checker 敘述 | checker 可驗證 |
| Production README | 新增 rollback drill 說明與 Makefile target | checker 可驗證 |
| API contract | 版本升到 v1.0.70，新增 rollback drill contract row | checker 可驗證 |
| OpenAPI | 版本升到 v1.0.70，補 rollback drill governance 描述 | checker 可驗證 |
| 章節與 Cheat Sheet | 補部署、測試與速查表入口 | checker 可驗證 |
| 整合視覺課程 | 補 release rollback drill 卡片與評分缺口更新 | checker 可驗證 |
| Inventory checker | 38 提升到 39，版本改 v1.0.70 | `node scripts/check-contract-gate-inventory-contract.mjs` 通過 |
| 發布首頁 | `golang-complete-visual-course.html` 同步到 `docs/index.html` | docs link checks 通過 |

## 不納入本輪

- 不新增 OAuth2、mTLS、API Gateway、SBOM 或簽章 runtime/CI 實作。
- 不改 production API runtime 行為。
- 不重寫既有章節架構。
- 不新增需要外部服務才能驗證的部署平台範本。
