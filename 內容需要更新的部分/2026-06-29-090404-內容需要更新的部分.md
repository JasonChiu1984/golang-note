# 內容需要更新的部分

- 產出日期：2026-06-29
- 完整日期時間：2026-06-29 09:04:04 CST +0800
- 參照審查報告：`審查報告/2026-06-29-090404-資深工程師審查報告.md`
- 目標版本：v1.0.73
- 更新主題：Prometheus config contract gate

## 必要更新

1. 新增正式 Prometheus config contract checker
   - 新增 `scripts/check-prometheus-config-contract.mjs`。
   - 檢查 `configs/prometheus/prometheus.yml` 的 `scrape_interval`、`evaluation_interval`、`rule_files`、`job_name`、`metrics_path`、`api:8080` 與 `environment: local-compose`。
   - 檢查 `configs/prometheus/production-api-worker-alerts.yml` 保留 5xx rate、queue depth、worker latency、metrics missing 等 alert rules。
   - 檢查 Compose `monitoring` profile、Prometheus port、config/rules mount 與 API key scrape auth 風險說明。

2. 更新 release gate wiring
   - `production-api-worker/Makefile` 的 `prometheus-check` 要執行正式 contract checker。
   - `.github/workflows/ci.yml` 要新增 `Check Prometheus config contract` step。
   - `scripts/check-contract-gate-inventory-contract.mjs` 與其他 inventory-sensitive checkers 要從 41 更新為 42。

3. 更新教材文件與合約文字
   - `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md` 要標示 `Prometheus config contract gate`。
   - `chapters/07-large-project-concurrent-crawler.md`、`chapters/09-build-and-deploy.md`、`chapters/10-performance-and-memory.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md` 與整合視覺課程要同步描述。
   - 明確說明 local teaching mode 可不帶 scrape auth；若 `API_KEY` 啟用，正式環境需用 bearer token file、secret mount 或平台原生 scrape auth。

4. 更新版本與 release artifact chain
   - `VERSION` 更新為 `v1.0.73`。
   - `CHANGELOG.md` 新增 v1.0.73 條目。
   - `production-api-worker/api/openapi.yaml` 與 `production-api-worker/docs/api-contract.md` 版本更新為 `v1.0.73`。
   - `scripts/check-release-artifact-chain-contract.mjs` 改為驗證本次 2026-06-29 artifact chain。

5. 同步發布入口
   - 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製/同步到 `docs/index.html`。
   - 執行 docs link sync/check，避免補充頁與 ReleaseNote link 遺失。

## 驗收項目

- `node --check scripts/check-prometheus-config-contract.mjs`
- `node scripts/check-prometheus-config-contract.mjs`
- `cd production-api-worker && make prometheus-check`
- 全部 root `scripts/check-*-contract.mjs` 通過
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML parse
- `cd production-api-worker && docker compose config`
- root 與 production focused Go tests
- `git diff --check`
