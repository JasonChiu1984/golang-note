# 內容需要更新的部分

建立日期時間：2026-06-24 06:02:30 CST +0800
對應審查報告：`審查報告/2026-06-24-060230-資深工程師審查報告.md`
目標版本：v1.0.68

## 必要修正

| 編號 | 更新項目 | 目標 |
|---|---|---|
| 1 | 新增 `scripts/check-worker-shutdown-contract.mjs` | 固定 worker queue shutdown safety 不只依賴 Go test selector |
| 2 | 更新 `production-api-worker/Makefile` | 新增 `worker-shutdown-check`，並讓 release gate 可直接執行 |
| 3 | 更新 `.github/workflows/ci.yml` | root-course gate 加入 Worker shutdown contract checker |
| 4 | 更新 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml` | 將 Worker shutdown contract 納入正式 project contract surface |
| 5 | 更新第 7 / 9 / 11 章、`Cheatsheet/cheatsheet-advanced.md`、`圖解筆記3-4整合/golang-complete-visual-course.html` | 讓教程章節與整合視覺課程可追蹤此 contract |
| 6 | 更新 inventory 與 version-sensitive checkers | root contract checker 從 36 更新為 37，版本升為 `v1.0.68` |
| 7 | 更新 `VERSION`、`CHANGELOG.md`、`docs/index.html` | 完成本輪 release hygiene 與 GitHub Pages 首頁同步 |

## 驗證條件

- `node --check scripts/check-worker-shutdown-contract.mjs`
- `node scripts/check-worker-shutdown-contract.mjs`
- `cd production-api-worker && make worker-shutdown-check`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- 所有 `scripts/check-*-contract.mjs` 通過
- `cd production-api-worker && make ci-contract` 通過
- `go test ./... -count=1` 通過
- `cd production-api-worker && go test ./... -count=1` 通過
- `node scripts/fix-docs-index-links.mjs --check` 與 `node scripts/check-html-home-links.mjs` 通過
- `git diff --check` 與 `git diff --cached --check` 通過
