# 內容需要更新的部分

- 產生日期：2026-07-14
- 完整日期時間：2026-07-14 06:01:43 CST +0800
- 依據審查報告：`審查報告/2026-07-14-060143-資深工程師審查報告.md`
- 目標版本：v1.0.88
- 本輪主題：Release artifact metadata consistency contract gate

## 必須更新

| 項目 | 更新內容 | 驗證方式 |
|---|---|---|
| 版本號 | `VERSION`、README、API contract、OpenAPI 與 version-sensitive checker 更新到 `v1.0.88` | `node scripts/check-release-version-consistency-contract.mjs` |
| 新增 checker | 新增 `scripts/check-release-artifact-metadata-contract.mjs`，固定三份 dated release artifacts 的日期、版本、主題與交叉引用 | `node --check` 與 checker 本身 |
| Makefile | 新增 `release-artifact-metadata-check` target，並納入 production contract 工具入口 | `cd production-api-worker && make release-artifact-metadata-check` |
| GitHub Actions | 新增 `Check release artifact metadata consistency contract` 步驟 | root checker sweep 與 inventory checker |
| Contract inventory | 從 52 個 root contract checker 提升到 53 個，更新 README、API contract、OpenAPI、章節、Cheatsheet、整合視覺課程 | `node scripts/check-contract-gate-inventory-contract.mjs` |
| 發版 artifact chain | 將本輪審查報告、內容需要更新的部分、更新紀錄納入 release artifact chain checker 與 metadata checker | `node scripts/check-release-artifact-chain-contract.mjs`、`node scripts/check-release-artifact-metadata-contract.mjs` |
| docs 首頁 | 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製到 `docs/index.html` 並修正 Pages links | `node scripts/fix-docs-index-links.mjs --check`、`node scripts/check-html-home-links.mjs` |

## 不建議本輪處理

| 項目 | 原因 |
|---|---|
| 新增大型章節 | 現有深度廣度已足夠，本輪缺口是 release artifact metadata governance |
| 變更 runtime behavior | Artifact metadata consistency 是 static governance，不需要改 production API worker runtime |
| 重寫 ReleaseNote baseline | 2026-07-14 官方 Go Release History 仍維持 `go1.26.5` / `go1.25.12`，不需刷新 patch baseline |

## 完成條件

1. `v1.0.88` 版本面全部同步。
2. 53 個 root contract checker 全部納入 GitHub Actions。
3. 本輪三份 dated artifacts 都存在、互相引用，且 metadata 一致。
4. `docs/index.html` 已由整合視覺課程來源同步。
5. 完成 commit、annotated tag 與 GitHub push；若網路阻擋，更新紀錄需明確寫出 local/remote/tag 狀態。

