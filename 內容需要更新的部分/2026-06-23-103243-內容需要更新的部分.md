# 內容需要更新的部分

- 日期：2026-06-23
- 完整日期時間：2026-06-23 10:32:43 CST +0800
- 來源審查報告：`審查報告/2026-06-23-103243-資深工程師審查報告.md`
- 目標版本：v1.0.67

## 本輪修正主題

將既有 trace provider shutdown 行為正式升級為 Trace shutdown contract gate。`production-api-worker` 已有 `Observability.Shutdown` 與 api-worker exit hook，但尚未以獨立 gate 防止 telemetry shutdown deadline、trace flush 與 lifecycle hook 在重構後漂移。

## 更新清單

1. 新增 `production-api-worker/internal/observability/observability_test.go`，加入 `TestTraceShutdownContract`。
2. 新增 `scripts/check-trace-shutdown-contract.mjs`，固定 trace shutdown code、test、docs、Makefile 與 CI surface。
3. 更新 `production-api-worker/Makefile`，新增 `trace-shutdown-check`，並讓 `ci-contract` 納入 observability focused test。
4. 更新 `.github/workflows/ci.yml`，新增 root checker step 與 production contract focused test。
5. 更新 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、章節、Cheat Sheet 與整合視覺課程，新增 Trace shutdown contract 說明與驗證入口。
6. 更新 `scripts/check-contract-gate-inventory-contract.mjs` 與 inventory-aware checkers，將 root contract checker 數量調整為 36。
7. 更新 `scripts/check-release-artifact-chain-contract.mjs`，固定本輪 timestamp、版本與 artifact chain。
8. 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製並同步為 `docs/index.html`，再執行 link fix。

## 驗證要求

- `node --check scripts/check-trace-shutdown-contract.mjs`
- `node scripts/check-trace-shutdown-contract.mjs`
- 全部 root `scripts/check-*-contract.mjs`
- `cd production-api-worker && make trace-shutdown-check`
- `cd production-api-worker && make ci-contract`
- root 與 production module 的 `go test ./... -count=1`
- docs index sync/check 與 HTML home-link check
- GitHub Actions YAML parse、Docker Compose config、`git diff --check`
