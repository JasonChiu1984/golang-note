# 內容需要更新的部分

> 更新日期：2026-07-21
> 完整日期時間：2026-07-21 06:01:21 CST +0800
> 來源審查報告：`審查報告/2026-07-21-060121-資深工程師審查報告.md`
> 對應版本：v1.0.94
> 本輪主題：Structured log schema governance contract gate

## 更新原因

資深工程師審查確認，教程針對專案開發的深度與廣度已符合中高階 Go production 教材要求。本輪需要更新的缺口集中在 structured log schema governance：現有教材已有 `slog`、`X-Request-ID`、trace correlation 與 incident artifact redaction，但尚未用獨立 root contract checker 固定 structured log 欄位 schema、redaction、retention 與 audit review cadence。

## 必須更新項目

1. 新增 `scripts/check-structured-log-schema-governance-contract.mjs`。
2. 在 `production-api-worker/Makefile` 新增 `structured-log-schema-governance-check`。
3. 在 `.github/workflows/ci.yml` 新增 `Check structured log schema governance contract`。
4. 將 `VERSION` 更新為 `v1.0.94`。
5. 更新 `CHANGELOG.md`，新增 `v1.0.94 - 2026-07-21`。
6. 同步下列教材與合約面到 `v1.0.94` 與本輪主題：
   - `README.md`
   - `production-api-worker/README.md`
   - `production-api-worker/docs/api-contract.md`
   - `production-api-worker/api/openapi.yaml`
   - `production-api-worker/docs/operational-runbook.md`
   - `chapters/07-large-project-concurrent-crawler.md`
   - `chapters/09-build-and-deploy.md`
   - `chapters/11-advanced-testing.md`
   - `Cheatsheet/cheatsheet-advanced.md`
   - `圖解筆記3-4整合/golang-complete-visual-course.html`
7. 更新 release / inventory checkers：
   - `scripts/check-release-version-consistency-contract.mjs`
   - `scripts/check-release-artifact-chain-contract.mjs`
   - `scripts/check-release-artifact-metadata-contract.mjs`
   - `scripts/check-release-artifact-continuity-contract.mjs`
   - `scripts/check-contract-gate-inventory-contract.mjs`
   - `scripts/check-docs-publishing-contract.mjs`
   - 其他寫死 root checker count 的既有 checker
8. 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製同步到 `docs/index.html`，再執行 `node scripts/fix-docs-index-links.mjs --sync-source`。

## 本輪需固定的 structured log governance 條款

- `request_id`
- `trace_id`
- `severity`
- `error_code`
- `route`
- log redaction policy
- log retention owner
- audit log review cadence

## 不在本輪範圍

- 不新增 logging backend。
- 不改 production runtime handler 行為。
- 不導入新的第三方 logging library。
- 不更新 Go ReleaseNote patch baseline，因 2026-07-21 06:01:21 CST +0800 查核官方 Go Release History 後仍維持 `go1.26.5` / `go1.25.12`。
