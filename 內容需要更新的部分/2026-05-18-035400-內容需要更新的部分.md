# 內容需要更新的部分

| 項目 | 內容 |
|---|---|
| 來源審查報告 | `審查報告/2026-05-18-035400-資深工程師審查報告.md` |
| 建立日期 | 2026-05-18 |
| 完整建立時間 | 2026-05-18 03:54:00 CST +0800 |
| 目標版本 | `v1.0.35` |

## 1. 必須修正

| 編號 | 內容 | 目標檔案 |
|---|---|---|
| R1 | 將 `api-worker` 監聽訊號抽出為可測的 `monitoredSignals()` | `production-api-worker/cmd/api-worker/main.go` |
| R2 | 固定 `SIGINT` 與 `SIGTERM` 都會進入 shutdown flow | `production-api-worker/cmd/api-worker/main.go` |
| R3 | 新增 `TestMonitoredSignalsContract` | `production-api-worker/cmd/api-worker/main_test.go` |
| R4 | 新增 `TRUSTED_PROXY_CIDRS` 與 trusted proxy client IP 邊界 | `production-api-worker/internal/config/config.go`、`production-api-worker/internal/api/rate_limit.go` |
| R5 | 新增 `TestRateLimitTrustedProxyContract`，固定未信任來源不可用 `X-Forwarded-For` 偽造 client IP | `production-api-worker/internal/api/handler_test.go` |
| R6 | 新增 shutdown signal 靜態合約檢查 | `scripts/check-shutdown-signal-contract.mjs` |
| R7 | 將 shutdown signal / trusted proxy gate 接入 Makefile、檢查腳本與 GitHub Actions | `production-api-worker/Makefile`、`scripts/check-rate-limit-contract.mjs`、`.github/workflows/ci.yml` |

## 2. 文件同步

| 文件 | 更新內容 |
|---|---|
| `README.md` | 新增 `v1.0.35`、Shutdown signal / Trusted proxy contract 與驗證指令 |
| `production-api-worker/README.md` | 新增 Shutdown Signal Contract、trusted proxy 設定、`TestMonitoredSignalsContract` 與 `make shutdown-signal-check` |
| `production-api-worker/docs/api-contract.md` | 新增 `SIGINT/SIGTERM` deployment contract、`TRUSTED_PROXY_CIDRS` 與 release gate |
| `chapters/07-large-project-concurrent-crawler.md` | 補上 shutdown signal contract 與 trusted proxy client IP 屬於 production lifecycle / API 合約 |
| `chapters/11-advanced-testing.md` | 補上 `TestMonitoredSignalsContract` 與 trusted proxy rate limit 測試策略 |
| `Cheatsheet/cheatsheet-advanced.md` | 補上 shutdown signal 與 trusted proxy release 前檢查 |
| `圖解筆記3-4整合/golang-complete-visual-course.html` | 補上整合課程中的 shutdown signal / trusted proxy walkthrough |

## 3. 自動化檢查

| 檢查 | 目的 |
|---|---|
| `node scripts/check-shutdown-signal-contract.mjs` | 固定文件、main.go、main_test.go 與 CI 都保留 shutdown signal 合約 |
| `node scripts/check-rate-limit-contract.mjs` | 固定 `TRUSTED_PROXY_CIDRS`、trusted proxy test、OpenAPI 與文件入口 |
| `make shutdown-signal-check` | production module 內的本機入口 |
| `.github/workflows/ci.yml` | root-course gate 自動執行 shutdown signal contract check |

## 4. 不在本輪處理

| 項目 | 原因 |
|---|---|
| 真實 Kubernetes termination grace period 實測 | 需要外部叢集環境 |
| load balancer connection draining 實測 | 需要實際 LB / ingress 設定 |
| worker 任務中斷補償策略 | 屬於更高階可靠性設計，本輪先固定 signal 入口 |
