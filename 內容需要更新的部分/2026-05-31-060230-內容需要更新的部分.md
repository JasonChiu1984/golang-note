# 內容需要更新的部分

> 來源審查報告：`審查報告/2026-05-31-060230-資深工程師審查報告.md`
> 建立日期：2026-05-31
> 完整日期時間：2026-05-31 06:02:30 CST +0800
> 目標版本：v1.0.44

## 更新清單

| 狀態 | 項目 | 檔案 |
|---|---|---|
| 已完成 | 新增 queue backpressure 靜態 gate，固定 bounded queue 滿載、`domain.ErrQueueFull`、API `503 queue_full`、dropped metric、Go test、文件與 CI 入口 | `scripts/check-queue-backpressure-contract.mjs` |
| 已完成 | 新增 `TestQueueBackpressureContract`，驗證 queue 滿載時第二個 enqueue 回 `domain.ErrQueueFull`、記錄 dropped result 並保留 queue depth | `production-api-worker/internal/worker/queue_test.go` |
| 已完成 | 將 queue backpressure gate 納入 Makefile 與 CI | `production-api-worker/Makefile`、`.github/workflows/ci.yml` |
| 已完成 | 更新版本、CHANGELOG、OpenAPI 與 API contract | `VERSION`、`CHANGELOG.md`、`production-api-worker/api/openapi.yaml`、`production-api-worker/docs/api-contract.md` |
| 已完成 | 同步主 README、production README、章節、Cheat Sheet 與整合視覺課程 | `README.md`、`production-api-worker/README.md`、`chapters/07-large-project-concurrent-crawler.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md`、`圖解筆記3-4整合/golang-complete-visual-course.html` |
| 已完成 | 重新同步 GitHub Pages 首頁 | `docs/index.html` |

## 驗收標準

```bash
node --check scripts/check-queue-backpressure-contract.mjs
node scripts/check-queue-backpressure-contract.mjs
cd production-api-worker && go test ./internal/worker -run 'TestQueueBackpressureContract' -count=1
cd production-api-worker && make queue-backpressure-check
```
