# 內容需要更新的部分

> 產出日期：2026-07-11
> 完整日期時間：2026-07-11 06:03:31 CST +0800
> 來源審查報告：`審查報告/2026-07-11-060331-資深工程師審查報告.md`
> 建議目標版本：v1.0.85
> 主題：Release publish recovery finalization

## 需要更新的內容

1. 發布狀態紀錄
   - 新增 v1.0.85 更新紀錄，明確記錄本輪已成功補推 `v1.0.84`。
   - 保留前置推送指令 `git push origin main refs/tags/v1.0.84`。
   - 保留推送結果 `main -> main` 與 `[new tag] v1.0.84 -> v1.0.84`。
   - 記錄 `HEAD`、`origin/main`、`v1.0.84^{}` 與本輪 `v1.0.85^{}` 的最終對齊狀態。

2. Release publish reconciliation gate
   - 更新 `scripts/check-release-publish-reconciliation-contract.mjs` 到 `v1.0.85`。
   - 要求 checker 驗證 `更新資料/2026-07-10-060242-v1.0.84-更新紀錄.md` 的 blocked-push 背景與本輪 `更新資料/2026-07-11-060331-v1.0.85-更新紀錄.md` 的 recovery finalization 證據。
   - 保留 `force-with-lease` 與 recovery command 條款，避免未來遇到 local-final-amended release 時覆蓋遠端歷史。

3. Release artifact chain
   - 更新 `scripts/check-release-artifact-chain-contract.mjs` 的 expected version 為 `v1.0.85`。
   - 更新 artifact timestamp 為 `2026-07-11-060331`。
   - 確認三份 artifact 互相引用：審查報告、內容需要更新的部分、更新資料。

4. 教材與合約文件
   - 更新 `VERSION`、`CHANGELOG.md`、`README.md`。
   - 更新 `production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`。
   - 更新 `chapters/07-large-project-concurrent-crawler.md`、`chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md`。
   - 更新 `圖解筆記3-4整合/golang-complete-visual-course.html`，再同步到 `docs/index.html`。

5. 驗證與發布
   - 執行 release-publish-reconciliation、release-artifact-chain、contract-gate-inventory 與全 root checker sweep。
   - 執行 docs link check、YAML parse、Docker Compose config、`git diff --check`。
   - 建立 v1.0.85 commit 與 annotated tag。
   - 推送 `main` 與 `v1.0.85` 到 GitHub。
