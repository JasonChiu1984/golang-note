# 2026-05-13 11:02 內容需要更新的部分

> 來源：`審查報告/2026-05-13-110234-資深工程師審查報告.md`  
> 目標版本：v1.0.14  
> 更新主題：Worker queue shutdown 併發安全與離線可測性

## 1. 程式碼更新

| 檔案 | 更新內容 |
|---|---|
| `production-api-worker/internal/worker/queue.go` | 讓 `Enqueue` 在 mutex 內完成 `closed` 檢查與 channel send，避免 `ShutdownContext` 於送入前 close channel |
| `production-api-worker/internal/worker/queue.go` | 新增 `ErrClosed`，讓 shutdown 後的 enqueue 回穩定錯誤 |
| `production-api-worker/internal/worker/queue.go` | 將 observability 依賴縮小為 `Observer` interface，避免 worker package 測試強制載入 Prometheus / OpenTelemetry |
| `production-api-worker/internal/observability/observability.go` | 新增 `ObserveWorkerQueueDepth`、`ObserveWorkerJobDuration`、`ObserveWorkerJobResult`，讓既有 metrics 行為保持不變 |
| `production-api-worker/internal/worker/queue_test.go` | 新增 close 後 enqueue 回 `ErrClosed` 的回歸測試 |
| `production-api-worker/internal/worker/queue_test.go` | 新增 concurrent enqueue + shutdown 不 panic 的回歸測試 |

## 2. 教材文件更新

| 檔案 | 更新內容 |
|---|---|
| `README.md` | 升級至 `v1.0.14`，新增 queue shutdown safety 版本策略與驗證指令 |
| `CHANGELOG.md` | 新增 `v1.0.14` 更新紀錄 |
| `VERSION` | 標註 `v1.0.14` |
| `chapters/07-large-project-concurrent-crawler.md` | 在 production API + worker 專案段落補上 queue close/send 同步邊界 |
| `chapters/11-advanced-testing.md` | 新增 worker shutdown safety 測試 gate |
| `Cheatsheet/cheatsheet-advanced.md` | 補上 graceful shutdown 的 queue close/send 規則與測試指令 |
| `康乃爾筆記法/07-大型專案架構.md` | 新增 queue shutdown race 問答 |
| `康乃爾筆記法/11-進階測試實務.md` | 新增 queue shutdown test 筆記 |
| `production-api-worker/README.md` | 補上 shutdown-safe enqueue / close 與 release quality gate |
| `production-api-worker/docs/api-contract.md` | 更新到 `v1.0.14`，把 queue lifecycle 納入 release gate |

## 3. 驗證項目

| 驗證 | 期待結果 |
|---|---|
| `gofmt` | Go 檔案格式化完成 |
| `cd production-api-worker && TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache go test ./internal/worker -count=1` | 通過 |
| `git diff --check` | 無 whitespace error |
| `rg "v1.0.14"` | README、VERSION、CHANGELOG、API contract 與更新紀錄可查到新版本 |

