# 內容需要更新的部分

產生日期時間：2026-06-04 06:02:21 CST +0800

依據：`審查報告/2026-06-04-060221-資深工程師審查報告.md`

## 必要更新

| 狀態 | 項目 | 修改目標 |
|---|---|---|
| 已完成 | Readiness lifecycle static gate | 新增 `scripts/check-readiness-contract.mjs`，固定 `/livez`、`/readyz`、draining、public probes、OpenAPI、README、章節、Makefile 與 CI |
| 已完成 | Makefile release gate | 新增 `readiness-check` target |
| 已完成 | GitHub Actions | 新增 `Check readiness contract` root-course step |
| 已完成 | 教材與 API 文件 | README、production README、API contract、OpenAPI、第 7 / 11 章與整合視覺課程補齊 Readiness lifecycle contract gate |
| 已完成 | 版本與 Pages | `VERSION`、`CHANGELOG.md`、version-sensitive checkers 與 `docs/index.html` 同步到 `v1.0.48` |

## 驗證需求

| 類型 | 指令 |
|---|---|
| 新 checker 語法 | `node --check scripts/check-readiness-contract.mjs` |
| 新 checker 執行 | `node scripts/check-readiness-contract.mjs` |
| Makefile gate | `cd production-api-worker && make readiness-check` |
| Contract suite | `cd production-api-worker && make ci-contract` |
| Docs sync | `node scripts/fix-docs-index-links.mjs --sync-source && node scripts/fix-docs-index-links.mjs --check` |
| HTML links | `node scripts/check-html-home-links.mjs` |
| YAML syntax | `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'` |
| Diff whitespace | `git diff --check` |
