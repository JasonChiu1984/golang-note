# 內容需要更新的部分：v1.0.36

| 項目 | 內容 |
|---|---|
| 產生日期 | 2026-05-18 |
| 完整日期時間 | 2026-05-18 06:25:05 CST +0800 |
| 對應審查報告 | `審查報告/2026-05-18-062505-資深工程師審查報告.md` |
| 目標版本 | `v1.0.36` |

## 必須更新

| 優先級 | 更新項目 | 目標 | 驗收方式 |
|---|---|---|---|
| P1 | 確認 `api-worker` 同時監聽 `SIGINT` / `SIGTERM` | 避免 Docker stop / rolling deploy 只剩本機 Ctrl+C path | `TestMonitoredSignalsContract` |
| P1 | 確認 trusted proxy client IP 邊界已納入 rate limit | 避免未信任來源用 `X-Forwarded-For` 偽造 client key | `TestRateLimitTrustedProxyContract` |
| P1 | 保留 `TRUSTED_PROXY_CIDRS` config、Compose、README、runbook 與 API contract | 讓部署與文件一致 | `node scripts/check-rate-limit-contract.mjs` |
| P1 | 確認 OpenAPI version 與 API contract 已對齊 `v1.0.35` runtime contract | 避免 machine-readable contract 漂移 | `node scripts/check-openapi-contract.mjs` |
| P1 | 確認 shutdown signal static gate 已接入 CI / Makefile | 避免文件與實作退化 | `node scripts/check-shutdown-signal-contract.mjs` |
| P1 | 由整合課程來源重建 `docs/index.html` | 保持 GitHub Pages 首頁同步 | `node scripts/fix-docs-index-links.mjs --check` |
| P1 | 建立本輪更新紀錄、commit、tag 並推送 GitHub | 保留可追溯發布證據 | `git push origin main refs/tags/v1.0.36` |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| 真實 Kubernetes rollout 測試 | 本地環境無正式叢集與 ingress；本輪僅交付可重跑 contract |
| 大規模 handler middleware 重構 | 目前風險可用最小幅度修正處理 |
| 新 API endpoint | 本輪目標是 production lifecycle 與 proxy 邊界收斂 |
