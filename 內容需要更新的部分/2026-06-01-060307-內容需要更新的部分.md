# 內容需要更新的部分

- 產出日期：2026-06-01
- 完整時間：2026-06-01 06:03:07 CST +0800
- 依據審查報告：`審查報告/2026-06-01-060307-資深工程師審查報告.md`
- 目標版本：v1.0.45

## 必須更新

| 項目 | 更新內容 | 驗收 |
|---|---|---|
| Migration static gate | 新增 `scripts/check-migration-contract.mjs` | `node --check` 與 `node scripts/check-migration-contract.mjs` 通過 |
| Makefile | 新增 `migration-check` target | `cd production-api-worker && make migration-check` 通過 |
| GitHub Actions | root-course 新增 `Check migration contract` | YAML 可解析，CI 入口保留 |
| README / production README | 補 Migration contract gate 與驗證指令 | `rg` 可找到 `node scripts/check-migration-contract.mjs` |
| API contract / OpenAPI | 更新 v1.0.45 與 migration gate 描述 | 既有 version-sensitive scripts 通過 |
| 第 7 / 11 章與 Cheat Sheet | 補 migration gate 教學與驗證矩陣 | 靜態 gate 可檢查到章節內容 |
| 整合視覺課程與 docs 首頁 | 加入 Migration contract gate，並同步 `docs/index.html` | docs sync/check 通過 |
| 版本紀錄 | 更新 `VERSION`、`CHANGELOG.md`、本更新紀錄 | tag `v1.0.45` 可追溯 |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| 真實 Postgres migration integration test | 需要 Docker / DB runtime；本輪為最小靜態 gate 收斂 |
| migration rollback 操作手冊 | 屬於後續 deployment runbook 深化，不影響本輪 gate |
| SBOM / 簽章 | 屬於供應鏈治理延伸題，非 migration contract 最小閉環 |
