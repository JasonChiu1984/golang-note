# 內容需要更新的部分

產出日期：2026-06-03
完整日期時間：2026-06-03 06:01:33 CST +0800
來源審查報告：`審查報告/2026-06-03-060133-資深工程師審查報告.md`
目標版本：v1.0.47

## 本輪必修項目

1. **Panic recovery static gate**
   - 新增 `scripts/check-panic-recovery-contract.mjs`。
   - 檢查 README、production README、handler、Go tests、OpenAPI、API contract、第 7 / 11 章、整合視覺課程、Makefile 與 CI workflow。

2. **Makefile / CI 入口**
   - `production-api-worker/Makefile` 新增 `panic-recovery-check`。
   - `.github/workflows/ci.yml` 新增 `Check panic recovery contract`。

3. **文件與 OpenAPI 同步**
   - README 新增 Panic recovery contract 策略與驗證列。
   - `production-api-worker/README.md` 將 Panic recovery 升級為正式 contract gate。
   - `production-api-worker/docs/api-contract.md` 加入 Panic recovery gate。
   - `production-api-worker/api/openapi.yaml` 版本與 description 更新為 `v1.0.47`。

4. **章節與整合課程**
   - 第 7 章補 panic recovery contract gate 的風險與驗證入口。
   - 第 11 章補 panic recovery contract gate 與 static check 指令。
   - `圖解筆記3-4整合/golang-complete-visual-course.html` 新增 Panic recovery contract card。

5. **Release traceability**
   - `VERSION` 更新為 `v1.0.47`。
   - `CHANGELOG.md` 新增 `v1.0.47 - 2026-06-03`。
   - `docs/index.html` 從整合課程重新同步。

## 驗證要求

- `node --check scripts/check-panic-recovery-contract.mjs`
- `node scripts/check-panic-recovery-contract.mjs`
- 既有 version-sensitive contract scripts
- `cd production-api-worker && make panic-recovery-check`
- `cd production-api-worker && make ci-contract`
- Root 與 `production-api-worker` 的 `go test ./... -count=1`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- `.github/workflows/ci.yml` YAML parse
- `git diff --check`
