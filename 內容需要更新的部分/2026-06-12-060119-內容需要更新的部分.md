# 內容需要更新的部分

- 產生日期：2026-06-12
- 完整日期時間：2026-06-12 06:01:19 CST +0800
- 依據審查報告：`審查報告/2026-06-12-060119-資深工程師審查報告.md`
- 目標版本：v1.0.56

## 本輪修正主題

將既有 Startup configuration fail-fast 行為升級成正式 static gate，避免 `PORT`、`QUEUE_SIZE`、`WORKERS`、`OTEL_EXPORTER_OTLP_ENDPOINT` 以及相關 env 驗證只存在於 Go tests 與文件敘述，未被 root checker、Makefile、GitHub Actions 與教材入口共同鎖住。

## 修改清單

| 類別 | 需要更新 | 驗收條件 |
|---|---|---|
| Static checker | 新增 `scripts/check-startup-config-contract.mjs` | 能檢查 README、production README、API contract、OpenAPI、config loader、config tests、章節、Cheat Sheet、整合課程、Makefile 與 CI |
| Makefile | 新增 `startup-config-check` | `cd production-api-worker && make startup-config-check` 可重跑 checker |
| CI | 新增 `Check startup config contract` | GitHub Actions root-course job 呼叫 `node scripts/check-startup-config-contract.mjs` |
| Contract inventory | 由 24 個 root checker 更新為 25 個 | `node scripts/check-contract-gate-inventory-contract.mjs` 能確認 startup config checker 已進 CI |
| 文件 | README、production README、API contract、OpenAPI、第 7 / 11 章、Cheat Sheet、整合視覺課程補上 Startup config contract gate | 使用者能從任一入口找到驗證指令與 fail-fast 邊界 |
| 版本 | 升級至 v1.0.56 | `VERSION`、`CHANGELOG.md`、API contract、OpenAPI、version-sensitive checks 一致 |
| Pages | `golang-complete-visual-course.html` 同步成 `docs/index.html` | docs index check 與 home-link check 通過 |

## 不在本輪範圍

- 不新增 production API runtime feature。
- 不更動 Go major/minor baseline；官方來源仍維持 `go1.26.4`。
- 不改動教學視覺設計，只補 release gate 與可追溯文字。
