# 內容需要更新的部分

- 產出日期：2026-06-21
- 完整日期時間：2026-06-21 06:01:59 CST +0800
- 對應版本：v1.0.65
- 依據審查報告：`審查報告/2026-06-21-060159-資深工程師審查報告.md`

## 本次需要更新

1. 新增 `scripts/check-api-latency-metrics-contract.mjs`，固定 API latency metrics contract gate。
2. 在 `production-api-worker/internal/observability/observability.go` 新增 `api_request_duration_seconds` histogram，標籤為 `route`、`method`、`status`。
3. 在 `production-api-worker/internal/api/handler.go` 的 metrics middleware 觀測 request duration，並保留 `api_requests_total`。
4. 在 `production-api-worker/internal/api/handler_test.go` 新增 `TestAPILatencyMetricsContract`，確認 Prometheus output 含 request counter 與 latency histogram。
5. 將 `api-latency-metrics-check` 接入 `production-api-worker/Makefile`、`.github/workflows/ci.yml` 與 contract inventory。
6. 同步 README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、Cheatsheet、整合視覺課程與 `docs/index.html`。
7. 將版本標記更新為 `v1.0.65`，並更新 `CHANGELOG.md`。

## 驗證要求

- `node --check scripts/check-api-latency-metrics-contract.mjs`
- `node scripts/check-api-latency-metrics-contract.mjs`
- all root `scripts/check-*-contract.mjs`
- `cd production-api-worker && make api-latency-metrics-check`
- `cd production-api-worker && make ci-contract`
- root / production Go tests
- docs index sync/check
- `git diff --check`
