# 內容需要更新的部分

> 日期：2026-05-19
> 完整日期時間：2026-05-19 15:50:00 CST +0800
> 對應審查報告：`審查報告/2026-05-19-155000-資深工程師審查報告.md`
> 目標版本：v1.0.38

## 必要更新

| 項目 | 修正內容 | 驗收方式 |
|---|---|---|
| Request body limit 設定 | 新增 `REQUEST_BODY_LIMIT_BYTES`，預設 1048576 bytes，錯誤值 fail fast | `go test ./internal/config -run TestLoadFromLookup -count=1` |
| API handler 行為 | `POST /jobs` oversized body 回 `413 payload_too_large` | `go test ./internal/api -run TestRequestBodyLimitContract -count=1` |
| OpenAPI contract | 新增 `PayloadTooLarge` response 與 `payload_too_large` error code | `node scripts/check-openapi-contract.mjs` |
| CI gate | 新增 `scripts/check-request-body-limit-contract.mjs` 與 workflow step | `node scripts/check-request-body-limit-contract.mjs` |
| 教程文件 | README、production README、API contract、第 7 / 11 章、Cheat Sheet、整合 HTML 同步說明 | `rg -n "REQUEST_BODY_LIMIT_BYTES|payload_too_large" ...` |
| GitHub Pages 入口 | 將整合 HTML 複製到 `docs/index.html` 並修正 Pages link | `node scripts/fix-docs-index-links.mjs --check` |

## 不納入本輪

| 項目 | 原因 |
|---|---|
| Idempotency-Key | 屬於下一個 API consistency layer，會影響 persistence / replay semantics |
| API gateway / WAF policy | 需要部署拓撲與外部設備設定，不適合與本輪 body limit 混合 |
| 壓測報告 | 本輪目標是 contract closure，非容量結論 |
