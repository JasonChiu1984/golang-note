# 內容需要更新的部分：Rate limit contract

> 產出日期：2026-05-17
> 完整日期時間：2026-05-17 20:42:15 CST +0800
> 對應審查報告：`審查報告/2026-05-17-204215-資深工程師審查報告.md`
> 目標版本：v1.0.34

## 1. 必要更新

| 優先級 | 更新項目 | 檔案 | 驗收方式 |
|---|---|---|---|
| P1 | 新增 `RATE_LIMIT_REQUESTS_PER_MINUTE` 設定與 fail-fast 驗證 | `production-api-worker/internal/config/config.go`、`config_test.go` | config focused test 通過 |
| P1 | 新增 per-client IP rate limiter | `production-api-worker/internal/api/rate_limit.go`、`handler.go` | `TestRateLimitContract` 通過 |
| P1 | 固定超限 response 為 `429 rate_limited` 與 `Retry-After: 60` | `handler_test.go`、`api/openapi.yaml`、`docs/api-contract.md` | Go test 與 OpenAPI gate 通過 |
| P1 | 新增 rate limit contract 檢查腳本 | `scripts/check-rate-limit-contract.mjs` | `node --check` 與執行腳本通過 |
| P1 | 將檢查納入 Makefile 與 CI | `production-api-worker/Makefile`、`.github/workflows/ci.yml` | `make rate-limit-check`、YAML parse |
| P2 | README / production README / runbook 補上操作說明 | `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/operational-runbook.md` | 關鍵字與 contract script 通過 |
| P2 | 章節與速查表同步補上 release gate | `chapters/07-*`、`chapters/11-*`、`Cheatsheet/cheatsheet-advanced.md` | docs gate 與 diff check |
| P2 | 整合視覺課程加入 Rate limit contract | `圖解筆記3-4整合/golang-complete-visual-course.html`、`docs/index.html` | docs index sync/check |

## 2. 不納入本輪

| 項目 | 原因 |
|---|---|
| Redis / distributed rate limit | 多副本全域限速需要外部基礎設施，超出本輪最小安全修正 |
| API gateway / service mesh 限速 | 屬部署平台策略，後續可獨立成章 |
| 動態調整限速 | 目前以啟動設定固定 contract，降低教材複雜度 |
| token bucket 演算法 | 本輪先以固定時間窗建立可測合約，後續可比較演算法差異 |

## 3. 驗證清單

```bash
node --check scripts/check-rate-limit-contract.mjs
node scripts/check-rate-limit-contract.mjs
cd production-api-worker && make rate-limit-check
cd production-api-worker && go test ./internal/config ./internal/api -run 'TestRateLimitContract|TestLoadFromLookup' -count=1
node scripts/check-openapi-contract.mjs
node scripts/fix-docs-index-links.mjs --sync-source
node scripts/fix-docs-index-links.mjs --check
node scripts/check-html-home-links.mjs
git diff --check
```
