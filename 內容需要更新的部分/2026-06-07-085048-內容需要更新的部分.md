# 內容需要更新的部分

產出日期：2026-06-07
完整產出時間：2026-06-07 08:50:48 CST +0800
對應審查報告：`審查報告/2026-06-07-085048-資深工程師審查報告.md`
目標版本：v1.0.51

## 更新結論

依照資深工程師審查，本輪不新增大型主題，而是把既有 `Request timeout` 行為升級為正式 static contract gate。更新後，handler deadline exceeded 的 `504 request_timeout` 行為不只由 `TestRequestTimeoutContract` 保護，也會由 Node static checker、Makefile、GitHub Actions、OpenAPI 與教學文件一起固定。

## 必須更新項目

| 項目 | 檔案 | 動作 |
|---|---|---|
| Static checker | `scripts/check-request-timeout-contract.mjs` | 新增，檢查 timeout contract 的程式、測試、文件、OpenAPI、Makefile 與 CI 入口 |
| Makefile | `production-api-worker/Makefile` | 新增 `request-timeout-check` |
| CI | `.github/workflows/ci.yml` | 新增 `Check request timeout contract` |
| README | `README.md` | 版本升級到 `v1.0.51`，新增 Request timeout contract gate 說明 |
| Production README | `production-api-worker/README.md` | Request Timeout Contract 補上 static gate 指令 |
| API contract | `production-api-worker/docs/api-contract.md` | 版本升級到 `v1.0.51`，補上 request timeout gate |
| OpenAPI | `production-api-worker/api/openapi.yaml` | 版本升級到 `v1.0.51` 並明確描述 request timeout response |
| 教學章節 | `chapters/07-large-project-concurrent-crawler.md`、`chapters/11-advanced-testing.md` | 補上 static gate 與 CI 入口 |
| Cheat Sheet | `Cheatsheet/cheatsheet-advanced.md` | 補上 `node scripts/check-request-timeout-contract.mjs` / `make request-timeout-check` |
| 視覺課程 | `圖解筆記3-4整合/golang-complete-visual-course.html` | 補上 request timeout contract gate 卡片 |
| Docs 首頁 | `docs/index.html` | 由整合課程同步並套用 link fix |
| 發版紀錄 | `CHANGELOG.md`、`VERSION`、`更新資料/2026-06-07-085048-v1.0.51-更新紀錄.md` | 記錄修改內容、驗證與 push/tag 狀態 |

## 驗收標準

- `node scripts/check-request-timeout-contract.mjs` 通過。
- `cd production-api-worker && make request-timeout-check` 通過。
- `cd production-api-worker && make ci-contract` 通過。
- `node scripts/check-openapi-contract.mjs` 通過。
- `node scripts/fix-docs-index-links.mjs --sync-source` 後，`node scripts/fix-docs-index-links.mjs --check` 通過。
- `node scripts/check-html-home-links.mjs` 通過。
- root 與 `production-api-worker` Go 測試通過，或清楚記錄環境阻斷原因。
- `git diff --check` 通過。
- 建立 release commit、annotated tag `v1.0.51`，並推送 GitHub。
