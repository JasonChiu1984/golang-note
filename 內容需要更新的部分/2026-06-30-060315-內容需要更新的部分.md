# 內容需要更新的部分

- 產出日期：2026-06-30
- 完整日期時間：2026-06-30 06:03:15 CST +0800
- 參照審查報告：`審查報告/2026-06-30-060315-資深工程師審查報告.md`
- 目標版本：v1.0.74
- 更新主題：OTLP export governance contract gate

## 必要更新

1. 新增正式 OTLP export governance contract checker
   - 新增 `scripts/check-otel-export-governance-contract.mjs`。
   - 檢查 local `debug exporter` 不被誤寫成 production backend。
   - 檢查正式環境替換 exporter 時保留 Tempo、Jaeger、OTLP backend 或雲端 APM 的 backend ownership 說明。
   - 檢查 runbook、README 與教材保留 sampling rate、retention window、sensitive attribute redaction 與 trace data ownership 條款。

2. 更新 release gate wiring
   - `production-api-worker/Makefile` 新增 `otel-export-governance-check`。
   - `.github/workflows/ci.yml` 新增 `Check OTLP export governance contract` step。
   - `scripts/check-contract-gate-inventory-contract.mjs` 與其他 inventory-sensitive checkers 要從 42 更新為 43。

3. 更新教材文件與合約文字
   - `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/operational-runbook.md`、`production-api-worker/docs/api-contract.md` 與 `production-api-worker/api/openapi.yaml` 要標示 `OTLP export governance contract gate`。
   - `chapters/07-large-project-concurrent-crawler.md`、`chapters/09-build-and-deploy.md`、`chapters/10-performance-and-memory.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md` 與整合視覺課程要同步描述。
   - 明確說明 local teaching mode 使用 `debug exporter`；正式環境需定義 backend、sampling rate、retention window、sensitive attribute redaction 與 owner。

4. 更新版本與 release artifact chain
   - `VERSION` 更新為 `v1.0.74`。
   - `CHANGELOG.md` 新增 v1.0.74 條目。
   - `production-api-worker/api/openapi.yaml` 與 `production-api-worker/docs/api-contract.md` 版本更新為 `v1.0.74`。
   - `scripts/check-release-artifact-chain-contract.mjs` 改為驗證本次 2026-06-30 artifact chain。

5. 同步發布入口
   - 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製/同步到 `docs/index.html`。
   - 執行 docs link sync/check，避免補充頁與 ReleaseNote link 遺失。

## 驗收項目

- `node --check scripts/check-otel-export-governance-contract.mjs`
- `node scripts/check-otel-export-governance-contract.mjs`
- `cd production-api-worker && make otel-export-governance-check`
- 全部 root `scripts/check-*-contract.mjs` 通過
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML parse
- `cd production-api-worker && docker compose config`
- root 與 production focused Go tests
- `git diff --check`
