# 2026-06-13 06:02:09 CST +0800 內容需要更新的部分

## 本輪修正目標

依據 `審查報告/2026-06-13-060209-資深工程師審查報告.md`，本輪更新目標是發布 `v1.0.57`，把既有 Operational runbook 與 Prometheus config 檢查升級為正式 `Operational observability contract gate`。

## 必改項目

| 項目 | 更新內容 | 驗收方式 |
|---|---|---|
| Static checker | 新增 `scripts/check-operational-observability-contract.mjs`，固定 runbook、Prometheus config、alert rules、Compose monitoring profile、README、API contract、章節、Makefile 與 CI 入口 | `node --check` 與 `node scripts/check-operational-observability-contract.mjs` 通過 |
| Makefile | 新增 `operational-observability-check` target | `cd production-api-worker && make operational-observability-check` 通過 |
| GitHub Actions | 新增 `Check operational observability contract` step | YAML parse 與 checker 通過 |
| Contract inventory | root contract checker 數量由 25 更新為 26，並納入新 checker | `node scripts/check-contract-gate-inventory-contract.mjs` 通過 |
| 文件同步 | README、production README、API contract、OpenAPI、chapters 7/9/11、Cheat Sheet、整合視覺課程加入新 gate | 對應 checker 與 `rg` 驗證通過 |
| 發版同步 | `VERSION`、`CHANGELOG.md`、version-sensitive scripts、`docs/index.html` 更新至 `v1.0.57` | docs sync/check、HTML home-link check、git diff check 通過 |

## 不做項目

- 不新增 runtime feature。
- 不改 Prometheus rule semantics。
- 不引入 Alertmanager、OTLP backend 或平台部署範本；這些保留為未來企業部署章節。
