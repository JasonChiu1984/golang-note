# 2026-06-11 06:01:26 CST +0800 內容需要更新的部分

## 修正目標

依據 `審查報告/2026-06-11-060126-資深工程師審查報告.md`，本輪以 `v1.0.55` 補齊 Contract gate inventory static gate。此修正不新增 runtime behavior，而是強化 release hygiene：所有 root `scripts/check-*-contract.mjs` 必須被 CI 明確呼叫，並在 Makefile、README、API contract、章節、進階速查與整合視覺課程中留下可追溯入口。

## 更新清單

| 狀態 | 項目 | 驗收方式 |
|---|---|---|
| 待更新 | 新增 `scripts/check-contract-gate-inventory-contract.mjs` | `node --check` 與 `node scripts/check-contract-gate-inventory-contract.mjs` 通過 |
| 待更新 | `production-api-worker/Makefile` 新增 `contract-gate-inventory-check` | `cd production-api-worker && make contract-gate-inventory-check` 通過 |
| 待更新 | `.github/workflows/ci.yml` 新增 `Check contract gate inventory contract` | YAML parse 通過，且 inventory checker 確認 CI 呼叫所有 checker |
| 待更新 | README / production README / API contract / OpenAPI 加入 Contract gate inventory gate | static checker 通過，版本標示更新為 `v1.0.55` |
| 待更新 | 第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程加入 inventory gate | static checker 通過，`docs/index.html` 由整合課程同步 |
| 待更新 | `VERSION`、`CHANGELOG.md` 與更新紀錄 | `git diff --check` 與 release artifact 檢查通過 |

## 不納入本輪的項目

- 不新增新的 HTTP endpoint、資料庫 schema 或 worker 行為。
- 不調整現有 production runtime contract，因為本輪審查時既有 `scripts/check-*-contract.mjs` 已全部通過。
- 不更新 Go 官方版本基準；本輪沒有外部 Go release note 變更需求。
