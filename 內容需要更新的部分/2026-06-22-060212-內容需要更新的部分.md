# 內容需要更新的部分

> 建立日期：2026-06-22
> 完整日期時間：2026-06-22 06:02:12 CST +0800
> 依據審查報告：`審查報告/2026-06-22-060212-資深工程師審查報告.md`
> 目標發版：v1.0.66

## 必要更新

本輪修正主題：Service transaction boundary contract gate。

| 狀態 | 更新項目 | 說明 |
|---|---|---|
| 已完成 | 新增 service transaction boundary static checker | 建立 `scripts/check-service-transaction-boundary-contract.mjs`，檢查 service 實作、Go test、README、API contract、OpenAPI、章節、Makefile 與 CI |
| 已完成 | 新增 Go contract test | 在 `production-api-worker/internal/app/service_test.go` 新增 `TestServiceTransactionBoundaryContract` |
| 已完成 | 更新 Makefile / CI | 加入 `service-transaction-boundary-check` 與 `Check service transaction boundary contract` |
| 已完成 | 更新 contract inventory | root contract checker 從 34 前進到 35 |
| 已完成 | 更新教材入口 | 同步 README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程 |
| 已完成 | 更新 release metadata | `VERSION`、`CHANGELOG.md`、version-sensitive checker 與發版紀錄更新為 `v1.0.66` |
| 已完成 | 同步 GitHub Pages 首頁 | 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製/同步到 `docs/index.html` 並套用 link fix |

## 驗證指令

```bash
node --check scripts/check-service-transaction-boundary-contract.mjs
node scripts/check-service-transaction-boundary-contract.mjs
cd production-api-worker && make service-transaction-boundary-check
cd production-api-worker && go test ./internal/app -run 'TestServiceTransactionBoundaryContract' -count=1
node scripts/check-contract-gate-inventory-contract.mjs
for script in scripts/check-*-contract.mjs; do node "$script"; done
node scripts/fix-docs-index-links.mjs --check
node scripts/check-html-home-links.mjs
```
