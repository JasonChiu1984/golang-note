# 內容需要更新的部分

> 產出日期：2026-07-04
> 完整日期時間：2026-07-04 06:03:01 CST +0800
> 對應審查報告：`審查報告/2026-07-04-060301-資深工程師審查報告.md`
> 目標版本：v1.0.78

## 1. 本輪更新主題

Operational runbook scope freshness contract gate。

本輪不新增 runtime feature，重點是把 `production-api-worker/docs/operational-runbook.md` 的文件日期、完整日期時間、適用範圍與近期 release governance surface 升級成 root contract checker 可驗證的正式 gate。

## 2. 必須更新

| 項目 | 需要更新內容 | 驗收方式 |
|---|---|---|
| Operational runbook metadata | 將 `production-api-worker/docs/operational-runbook.md` 文件日期與完整日期時間更新到 2026-07-04 06:03:01 CST +0800，並標示 v1.0.78 scope freshness。 | 新 checker 必須要求日期、完整日期時間與 v1.0.78。 |
| Runbook 適用範圍 | 首段 scope 需列入 API contract scope coverage、Docs publishing contract gate、Release artifact chain contract gate、Dependency governance、Secret handling governance、Supply chain artifact governance。 | `node scripts/check-operational-runbook-scope-contract.mjs` 通過。 |
| Root contract checker | 新增 `scripts/check-operational-runbook-scope-contract.mjs`，固定 README、production README、API contract、OpenAPI、runbook、章節、Cheat Sheet、整合視覺課程、Makefile 與 CI。 | `node --check` 與 runtime checker 通過。 |
| Makefile / CI | 新增 `operational-runbook-scope-check` target，GitHub Actions 加入 `Check operational runbook scope contract`。 | `cd production-api-worker && make operational-runbook-scope-check` 通過，inventory checker 能找到 CI call。 |
| Inventory | root contract checker inventory 從 45 更新到 46。 | `node scripts/check-contract-gate-inventory-contract.mjs` 通過。 |
| 教材入口 | 同步 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、chapters 7/9/11、Cheatsheet 與整合視覺課程。 | 全 root contract checker sweep 通過。 |
| 發版資訊 | `VERSION`、`CHANGELOG.md` 與 version-sensitive checker 更新為 v1.0.78。 | `node scripts/check-release-artifact-chain-contract.mjs` 通過。 |
| GitHub Pages 首頁 | 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`，並跑 link fix。 | `node scripts/fix-docs-index-links.mjs --check` 與 `node scripts/check-html-home-links.mjs` 通過。 |

## 3. 不做事項

- 不新增新的 API endpoint。
- 不改動 production runtime 行為。
- 不引入新的外部服務或部署平台。
- 不手動更新 Go ReleaseNote patch baseline；官方 Go Release History 目前仍是 Go 1.26.4 / Go 1.25.11。

## 4. 預期驗證命令

```bash
node --check scripts/check-operational-runbook-scope-contract.mjs
node scripts/check-operational-runbook-scope-contract.mjs
cd production-api-worker && make operational-runbook-scope-check
for f in scripts/check-*-contract.mjs; do node "$f"; done
node scripts/check-release-artifact-chain-contract.mjs
node scripts/check-contract-gate-inventory-contract.mjs
node scripts/fix-docs-index-links.mjs --sync-source
node scripts/fix-docs-index-links.mjs --check
node scripts/check-html-home-links.mjs
cd production-api-worker && docker compose config
git diff --check
```
