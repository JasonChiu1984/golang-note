# 2026-06-19 06:03:24 CST +0800 內容需要更新的部分

## 依據

參照 `審查報告/2026-06-19-060324-資深工程師審查報告.md`，本輪更新集中處理 dependency governance 尚未被正式 static gate 固定的缺口。

## 需要更新

1. 新增 Dependency governance contract checker。
   - 新增 `scripts/check-dependency-governance-contract.mjs`。
   - 檢查 root module 與 `production-api-worker` 的 dependency integrity、dependency update discovery、vulnerability scan 與離線處理邊界。
   - 固定 `go mod tidy`、`go mod verify`、`go list -m -u all`、`govulncheck ./...`、module proxy / vulnerability database 的網路限制說明。

2. 接入 Makefile 與 GitHub Actions。
   - 在 `production-api-worker/Makefile` 新增 `dependency-governance-check`。
   - 在 `.github/workflows/ci.yml` 新增 `Check dependency governance contract`。

3. 更新版本與文件入口。
   - `VERSION`、`README.md`、`CHANGELOG.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml` 更新為 `v1.0.63`。
   - README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程補上 Dependency governance contract gate。

4. 更新 contract gate inventory。
   - root contract checker 數量由 31 個更新為 32 個。
   - 確認 `scripts/check-contract-gate-inventory-contract.mjs`、CI 與教材文件同步。

5. 發布同步與驗證。
   - 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`。
   - 執行 dependency governance check、docs link checks、contract checks、Go tests、Docker Compose config 與 diff hygiene。
