# 內容需要更新的部分

- 產生日期：2026-07-05
- 完整日期時間：2026-07-05 06:03:40 CST +0800
- 參照審查報告：`審查報告/2026-07-05-060340-資深工程師審查報告.md`
- 目標版本：v1.0.79

## 需要更新的項目

1. 新增 Platform promotion policy contract gate：
   - 新增 `scripts/check-platform-promotion-policy-contract.mjs`
   - 固定 `platform promotion policy`
   - 固定 `environment approval`
   - 固定 `progressive rollout`
   - 固定 `platform-native signing`
   - 固定 `artifact verification`
   - 固定 `rollback owner`

2. 同步 production-api-worker release gate：
   - `production-api-worker/Makefile` 新增 `platform-promotion-policy-check`
   - `.github/workflows/ci.yml` 新增 `Check platform promotion policy contract`
   - `production-api-worker/README.md` 新增 Platform Promotion Policy Contract 說明與驗證命令
   - `production-api-worker/docs/operational-runbook.md` 更新 scope 與 verification table
   - `production-api-worker/docs/api-contract.md` 與 `production-api-worker/api/openapi.yaml` 加入 machine-readable / human-readable contract 條款

3. 同步教材與首頁：
   - `README.md` 更新本次更新重點與版本策略
   - `chapters/09-build-and-deploy.md` 補入部署平台 promotion policy 邊界
   - `chapters/11-advanced-testing.md` 補入測試與 release gate 說明
   - `Cheatsheet/cheatsheet-advanced.md` 補入重跑指令
   - `圖解筆記3-4整合/golang-complete-visual-course.html` 更新工具列與 CI/CD 評分
   - 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`

4. 更新版本與 contract inventory：
   - `VERSION` 更新為 `v1.0.79`
   - `CHANGELOG.md` 新增 `v1.0.79`
   - root contract checker inventory 從 46 更新為 47
   - `scripts/check-release-artifact-chain-contract.mjs` 指向本次 timestamp artifact chain

## 驗證要求

- `node --check scripts/check-platform-promotion-policy-contract.mjs`
- `node scripts/check-platform-promotion-policy-contract.mjs`
- `cd production-api-worker && make platform-promotion-policy-check`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- 全部 root `scripts/check-*-contract.mjs`
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- `.github/workflows/ci.yml` YAML parse
- `cd production-api-worker && docker compose config`
- `git diff --check`
