# 內容需要更新的部分

- 參照報告：`審查報告/2026-05-12-233624-資深工程師審查報告.md`
- 更新版本：`v1.0.2`
- 更新時間：2026-05-12 23:36:24 +0800

## 已完成更新

1. README 新增教材版本、雙專案學習路線與驗證指令。
2. README 明確納入 `production-api-worker`，補上「crawler -> production service」進階路徑。
3. 第 7 章新增第二階段專案導讀，補齊大型專案深度入口。
4. 第 11 章新增實務驗證指令矩陣與受限環境排錯表。
5. 新增 `VERSION` 與 `CHANGELOG.md`，建立版本化更新基準。

## 仍需持續追蹤

1. `production-api-worker` 的首次依賴下載需要可連外網路環境，之後可再補一版離線驗證策略。
2. `project-concurrent-crawler` 在本機 `go test` 仍受 `dyld` 測試 binary 問題影響，需在另一個乾淨 toolchain 再驗一次。
