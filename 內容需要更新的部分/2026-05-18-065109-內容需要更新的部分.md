# 內容需要更新的部分

| 項目 | 內容 |
|---|---|
| 來源審查報告 | `審查報告/2026-05-18-065109-資深工程師審查報告.md` |
| 建立日期 | 2026-05-18 |
| 完整建立時間 | 2026-05-18 06:51:09 CST +0800 |
| 目標版本 | `v1.0.36` |

## 1. 必須修正

| 編號 | 內容 | 目標檔案 |
|---|---|---|
| R1 | 新增 `CORS_ALLOWED_ORIGINS` 設定與 fail-fast 驗證 | `production-api-worker/internal/config/config.go` |
| R2 | 新增 CORS allowlist middleware 與 handler option | `production-api-worker/internal/api/handler.go` |
| R3 | 新增 CORS contract 測試與靜態檢查 | `production-api-worker/internal/api/handler_test.go`、`scripts/check-cors-contract.mjs` |
| R4 | 將 README / VERSION / CHANGELOG 標示前進到 `v1.0.36` 並補 CORS gate | `README.md`、`VERSION`、`CHANGELOG.md` |
| R5 | 同步 OpenAPI、API contract、Compose、Makefile 與 CI | `production-api-worker/api/openapi.yaml`、`.github/workflows/ci.yml` |
| R6 | 重新同步 `golang-complete-visual-course.html` 至 `docs/index.html` 並套用 Pages link fix | `docs/index.html` |

## 2. 文件同步

| 文件 | 更新內容 |
|---|---|
| `README.md` | 標示 `v1.0.36`、CORS allowlist contract 與檢查指令 |
| `CHANGELOG.md` | 新增 `v1.0.36` CORS allowlist contract 條目 |
| `VERSION` | 更新為 `v1.0.36` |
| `production-api-worker/README.md` | 新增 CORS Allowlist Contract、env、test gate 與 release gate |
| `production-api-worker/docs/api-contract.md` | 新增 CORS allowlist 外部合約 |
| `docs/index.html` | 重新同步 GitHub Pages 首頁 |

## 3. 自動化檢查

| 檢查 | 目的 |
|---|---|
| `node scripts/check-shutdown-signal-contract.mjs` | 確認 `v1.0.35` shutdown signal contract 仍保留 |
| `node scripts/check-rate-limit-contract.mjs` | 確認 `v1.0.35` rate limit / trusted proxy contract 仍保留 |
| `node scripts/check-openapi-contract.mjs` | 確認 OpenAPI contract 仍可解析 |
| `node scripts/check-cors-contract.mjs` | 固定 CORS allowlist 文件、測試、Compose、OpenAPI 與 CI 入口 |
| `node scripts/fix-docs-index-links.mjs --check` | 確認 Pages 首頁連結仍可解析 |

## 4. 不在本輪處理

| 項目 | 原因 |
|---|---|
| 真實 Kubernetes termination grace period 實測 | 需要外部叢集環境 |
| load balancer connection draining 實測 | 需要實際 LB / ingress 設定 |
| API gateway CORS 實測 | 需要 Nginx / Envoy / ingress 部署環境 |
| worker 任務中斷補償策略 | 屬於更高階可靠性設計，本輪先固定 signal 與 client identity 邊界 |
