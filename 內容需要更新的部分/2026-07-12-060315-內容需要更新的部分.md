# 內容需要更新的部分

建立日期：2026-07-12
完整日期時間：2026-07-12 06:03:15 CST +0800
對應審查報告：2026-07-12-060315-資深工程師審查報告.md
目標版本：v1.0.86
更新主題：Go ReleaseNote freshness evidence

## 必要更新

| 項目 | 更新內容 | 驗收方式 |
|---|---|---|
| Freshness checker | 新增 `scripts/check-go-release-notes-freshness-contract.mjs`，固定官方 Go Release History 查核日期、最新 patch 維持不變、Go 1.26.5 / Go 1.25.12 token 與入口文件 | `node scripts/check-go-release-notes-freshness-contract.mjs` |
| ReleaseNote index | 將 `ReleaseNote/index.html` 與 `docs/ReleaseNote/index.html` 的支援狀態日期推進到 2026-07-12，並加入官方查核證據文字 | `node scripts/check-go-release-notes-contract.mjs` 與 freshness checker |
| Generator source | 更新 `scripts/generate-go-release-notes.mjs` 的 `supportStatus.asOf` 與 freshness evidence 文字，避免未來重產時回退 | `node --check scripts/generate-go-release-notes.mjs` |
| 教材入口 | README、production README、API contract、OpenAPI、第 9 / 11 章、Cheatsheet、整合視覺課程加入 Go ReleaseNote freshness evidence gate | root contract sweep |
| Makefile / CI | 將 `go-release-notes-freshness-check` 加入 `production-api-worker/Makefile` 與 `.github/workflows/ci.yml` | `cd production-api-worker && make go-release-notes-freshness-check` |
| Inventory | Contract gate inventory 從 50 推進到 51 個 root contract checker | `node scripts/check-contract-gate-inventory-contract.mjs` |
| 發版鏈 | `VERSION`、`CHANGELOG.md`、artifact-chain checker、docs/index 同步到 v1.0.86 | `node scripts/check-release-artifact-chain-contract.mjs` |

## 不在本輪處理

- 不新增 Go patch baseline；官方 Go Release History 於 2026-07-12 查核時仍維持 `go1.26.5` / `go1.25.12`。
- 不新增 production-api-worker runtime feature。
- 不重寫既有 ReleaseNote 個別版本頁內容，只補 freshness evidence 與發版治理。
