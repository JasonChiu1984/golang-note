# 內容需要更新的部分

- 參照報告：`審查報告/2026-05-13-010325-資深工程師審查報告.md`
- 更新版本：`v1.0.4`
- 更新時間：2026-05-13 01:03:25 +0800

## 已完成更新

1. README 教材版本更新為 `v1.0.4`，並將本輪更新定位為輔助教材一致性修正。
2. `VERSION` 與 `CHANGELOG.md` 更新到 `v1.0.4`。
3. 康乃爾筆記索引新增 Go 1.26.3 版本基準與相容層說明。
4. 康乃爾筆記各章 header 從 Go 1.22 同步為 Go 1.26.3。
5. 康乃爾第 4 章補上 Go 1.26 `errors.AsType` 與泛型自我參照約束。
6. 康乃爾第 6 章補上 Go 1.25+ container-aware `GOMAXPROCS`。
7. 康乃爾第 10 章補上 Go 1.26 Green Tea GC 的工程判讀方式。
8. 康乃爾第 11 章補上 Go 1.25 `testing/synctest` 與 Go 1.26 `T.ArtifactDir`。
9. 康乃爾 A1 附錄補上 Go 1.26 `new(expression)` 與泛型自我參照約束。
10. 進階 Cheat Sheet 更新為 Go 1.20-1.26，補上 runtime/testing/toolchain 速查並修正 `cmp.Ordered`。
11. 圖解筆記4與圖解筆記3-4整合頁的 production 補強版本訊號更新為 Go 1.21-1.26，並加入 runtime/testing 卡片。

## 尚未更新或刻意保留

1. 根目錄與 `production-api-worker` 的 `go.mod` 仍保留 `go 1.22`，原因是目前本機 toolchain 仍是 Go 1.22.2，本輪只修教材內容一致性。
2. `production-api-worker` 依賴未做 dependency refresh，因目前網路/DNS 可能無法穩定下載 module。
3. Go 1.26 新 API 的可編譯驗證仍需在 Go 1.26.3 toolchain 補跑。

## 證據來源

- 官方 release history：Go 1.26.3 於 2026-05-07 釋出。
- 官方 Go 1.26 release notes：包含 `new(expression)`、泛型自我參照約束、`go fix` modernizers、Green Tea GC、`T.ArtifactDir`。
- 官方 Go 1.25 release notes：包含 container-aware `GOMAXPROCS` 與 `testing/synctest`。
