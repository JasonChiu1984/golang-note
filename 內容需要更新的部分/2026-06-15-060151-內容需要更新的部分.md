# 內容需要更新的部分

- 建立日期：2026-06-15
- 完整建立時間：2026-06-15 06:01:51 CST +0800
- 對應審查報告：`審查報告/2026-06-15-060151-資深工程師審查報告.md`
- 目標版本：v1.0.59

## 需要更新的內容

### 1. Production workflow contract gate

- 新增 `scripts/check-production-workflow-contract.mjs`。
- 檢查 `production-api-worker/.github/workflows/production-api-worker.yml` 是否保留：
  - path trigger：`production-api-worker/**`
  - Go setup：`go-version-file: production-api-worker/go.mod`
  - dependency gate：`go mod download`、`go mod verify`
  - contract gate：`make ci-contract`
  - race / coverage：`go test -race -cover ./... -count=1`
  - binary build：`go build ./cmd/api-worker`、`go build ./cmd/migrate`
  - vulnerability scan：`govulncheck ./...`
  - Docker build：`docker build -t production-api-worker:standalone .`
  - Compose smoke：`docker compose up -d --build`、`./scripts/compose-smoke.sh`
  - failure diagnostics：`docker compose logs --no-color`
  - cleanup：`docker compose down -v`

### 2. Workflow 與 CI / Makefile wiring

- 更新 `production-api-worker/.github/workflows/production-api-worker.yml`，使 standalone workflow 與 root release gate 的 production 品質要求對齊。
- `production-api-worker/Makefile` 新增 `production-workflow-check`。
- `.github/workflows/ci.yml` 新增 `Check production workflow contract`。
- `scripts/check-contract-gate-inventory-contract.mjs` 更新 root checker inventory：27 -> 28。

### 3. 教材與合約文件同步

- `README.md`：
  - 教材版本升為 `v1.0.59`。
  - 新增 Production workflow contract gate 說明。
  - 更新 contract checker 數量為 28。
- `production-api-worker/README.md`：
  - 新增 Production Workflow Contract 說明與本機命令。
- `production-api-worker/docs/api-contract.md`：
  - 版本升為 `v1.0.59`。
  - 新增 production standalone workflow contract 條款。
- `production-api-worker/api/openapi.yaml`：
  - version 升為 `v1.0.59`。
  - 新增 production workflow contract traceability wording。
- `chapters/07-large-project-concurrent-crawler.md`、`chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md`、`圖解筆記3-4整合/golang-complete-visual-course.html`：
  - 新增或同步 Production workflow contract gate 入口。

### 4. Release metadata

- `VERSION` 更新為 `v1.0.59`。
- `CHANGELOG.md` 新增 `v1.0.59 - 2026-06-15`。
- 既有 version-sensitive contract scripts 更新到 `v1.0.59`。
- 重新同步 `docs/index.html`。

## 驗證需求

- `node --check scripts/check-production-workflow-contract.mjs`
- `node scripts/check-production-workflow-contract.mjs`
- `cd production-api-worker && make production-workflow-check`
- 全部 root `scripts/check-*-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML / OpenAPI YAML / Prometheus YAML parse
- `git diff --check`
- root `go test ./... -count=1`
- `cd production-api-worker && make ci-contract`
- `cd production-api-worker && go test ./... -count=1`
- `cd production-api-worker && docker compose config`

## 發布需求

- 建立更新紀錄於 `更新資料/`，檔名包含目前日期時間與版本號。
- 複製 / 同步 `圖解筆記3-4整合/golang-complete-visual-course.html` 到 `docs/index.html`，並套用 link fix。
- 建立 release commit。
- 建立 annotated tag `v1.0.59`。
- 推送 `main` 與 `v1.0.59` 到 GitHub。
