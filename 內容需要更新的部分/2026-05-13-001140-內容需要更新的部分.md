# 內容需要更新的部分

- 參照報告：`審查報告/2026-05-13-001140-資深工程師審查報告.md`
- 更新版本：`v1.0.3`
- 更新時間：2026-05-13 00:11:40 +0800

## 已完成更新

1. README 教材版本更新為 `v1.0.3`，教材講解基準更新為 Go 1.26.3。
2. README 新增版本策略，明確區分「教材講解基準」與「範例 module 的 Go 1.22 相容層」。
3. 第 1 章更新 Go 1.26.3 環境基準、版本管理工具指令與新專案 `go.mod` 範例。
4. 第 6 章補上 Go 1.25+ container-aware `GOMAXPROCS`，修正舊版「預設等於 CPU 核數」的簡化說法。
5. 第 10 章補上 Go 1.26 Green Tea GC 預設啟用與量測建議。
6. 第 11 章補上 `testing/synctest`、`T.ArtifactDir` 與對應驗證指令。
7. 第 4 章與 A1 附錄補上 Go 1.26 `errors.AsType`、`new(expression)` 與泛型自我參照約束。
8. 第 9 章 Docker / GitHub Actions 範例更新為 Go 1.26.x。
9. `CHANGELOG.md` 與 `VERSION` 更新到 `v1.0.3`。

## 尚未更新或刻意保留

1. 根目錄與 `production-api-worker` 的 `go.mod` 仍保留 `go 1.22`，原因是本輪目標是教材內容現代化；目前本地 toolchain 是 Go 1.22.2，直接升 module baseline 會讓本地驗證更早失敗。
2. `production-api-worker` 依賴版本未升級，因目前環境無法穩定連外下載 module；後續應在有網路的 Go 1.26.3 環境另開 dependency refresh。
3. Go 1.26 新語法與 `testing/synctest` / `T.ArtifactDir` 尚未以本機 `go test` 實測，需在 Go 1.26.3 toolchain 補驗。

## 證據來源

- 官方 release history：Go 1.26.3 於 2026-05-07 釋出。
- 官方 Go 1.26 release notes：包含 `new(expression)`、Green Tea GC、`T.ArtifactDir` 等更新。
- 官方 Go 1.25 release notes：包含 container-aware `GOMAXPROCS` 與 `testing/synctest`。
