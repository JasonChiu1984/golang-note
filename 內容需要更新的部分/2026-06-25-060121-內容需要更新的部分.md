# 2026-06-25 06:01:21 CST +0800 內容需要更新的部分

依據 `審查報告/2026-06-25-060121-資深工程師審查報告.md`，本輪更新聚焦在將效能 benchmark governance 正式升級為可驗證 Performance benchmark governance contract gate，目標版本為 v1.0.69。

## 必要更新清單

| 狀態 | 項目 | 目標 |
|---|---|---|
| 待更新 | 新增 root static checker | 建立 `scripts/check-performance-benchmark-governance-contract.mjs`，固定 benchmark A/B、benchstat、pprof、metrics 與 release evidence 條款。 |
| 待更新 | Makefile / CI | 新增 `performance-benchmark-governance-check`，並在 `.github/workflows/ci.yml` 加入 `Check performance benchmark governance contract`。 |
| 待更新 | 教材與 API contract | 更新 README、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、OpenAPI、章節 7/9/10/11、Cheat Sheet 與整合視覺課程。 |
| 待更新 | 版本與 inventory | 將版本標記更新為 `v1.0.69`，root contract checker 數量從 37 更新為 38。 |
| 待更新 | 發版 artifact chain | 更新 CHANGELOG、release artifact chain checker、`docs/index.html` 與本更新紀錄。 |

## 驗證要求

- `node --check scripts/check-performance-benchmark-governance-contract.mjs`
- `node scripts/check-performance-benchmark-governance-contract.mjs`
- 全部 root `scripts/check-*-contract.mjs`
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML parse
- `cd production-api-worker && docker compose config`
- 依可用環境執行 Go focused/root verification
- `git diff --check` 與 `git diff --cached --check`
