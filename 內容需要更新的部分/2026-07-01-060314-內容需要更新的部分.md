# 內容需要更新的部分

建立日期：2026-07-01
完整建立時間：2026-07-01 06:03:14 CST +0800
依據審查報告：`審查報告/2026-07-01-060314-資深工程師審查報告.md`
目標版本：v1.0.75
更新主題：Secret handling governance contract gate

## 必要更新

| 項目 | 更新內容 | 驗收方式 |
|---|---|---|
| 新增 static checker | 建立 `scripts/check-secret-handling-governance-contract.mjs`，固定 secret rotation owner、no hard-coded production credentials、redaction、bearer token file / secret mount、incident artifact handling 與教學範例限制 | `node --check scripts/check-secret-handling-governance-contract.mjs` 與 `node scripts/check-secret-handling-governance-contract.mjs` |
| Makefile 入口 | 在 `production-api-worker/Makefile` 新增 `secret-handling-governance-check` | `cd production-api-worker && make secret-handling-governance-check` |
| CI 入口 | 在 `.github/workflows/ci.yml` 新增 `Check secret handling governance contract` | GitHub Actions root course job 呼叫新 checker |
| README / production README | 補上 Secret handling governance contract gate，說明 `API_KEY`、`PPROF_TOKEN`、Prometheus scrape auth、secret mount、rotation owner 與 redaction 邊界 | 新 checker 驗證必要詞彙 |
| Runbook | 補上 incident 期間 profile/log/trace artifact 不可保存 raw secret，正式環境需用 secret manager 或平台 secret mount | 新 checker 驗證 runbook |
| API contract / OpenAPI | 把 secret governance 納入正式 contract surface | `node scripts/check-openapi-contract.mjs` 與新 checker |
| 章節 / Cheat Sheet / 視覺課程 | 補上 `node scripts/check-secret-handling-governance-contract.mjs` 與 `make secret-handling-governance-check` | 新 checker 與 docs index sync |
| Inventory / artifact chain | root contract checker 數量更新為 44，release artifact chain 更新為本輪 timestamp 與 v1.0.75 | `node scripts/check-contract-gate-inventory-contract.mjs` 與 `node scripts/check-release-artifact-chain-contract.mjs` |
