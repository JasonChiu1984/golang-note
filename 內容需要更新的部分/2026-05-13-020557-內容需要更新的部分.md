# 內容需要更新的部分

- 產出時間：2026-05-13 02:05:57 +0800
- 對應審查報告：`審查報告/2026-05-13-020557-資深工程師審查報告.md`
- 目標版本：`v1.0.5`

## 更新清單

| 狀態 | 檔案 | 更新內容 |
|---|---|---|
| 已完成 | `README.md` | 教材版本升級為 `v1.0.5`，並把 Go 1.26 artifact 驗證指令改為 `go1.26.3 test -artifacts -outputdir ./test-artifacts ./...`。 |
| 已完成 | `VERSION` | 版本號更新為 `v1.0.5`。 |
| 已完成 | `CHANGELOG.md` | 新增 v1.0.5 更新紀錄，說明本輪修正 `errors.AsType` 與 artifact 指令。 |
| 已完成 | `Cheatsheet/cheatsheet-advanced.md` | 修正 `errors.AsType` 範例為 `(value, ok)` 寫法，並新增 `go test -artifacts -outputdir` 指令範例。 |
| 已完成 | `康乃爾筆記法/04-函式錯誤介面泛型.md` | 補充 `errors.AsType` 標準庫簽名會回傳 `(E, bool)`。 |
| 已完成 | `chapters/11-advanced-testing.md` | 修正 Go 1.26 artifact 測試指令，避免把輸出資料夾誤當 package argument。 |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| 升級 `go.mod` 的 `go 1.22` | 目前仍作為舊環境學習相容層；本輪只修教材正確性。 |
| 新增 Go 1.26-only 可執行測試檔 | 本機 toolchain 為 Go 1.22.x，新增檔案會破壞現有測試相容性。 |
| dependency refresh | 本輪沒有 dependency 變更需求。 |
