# 2026-06-27 06:01:55 CST +0800 內容需要更新的部分

## 本次修正主題

依據 `審查報告/2026-06-27-060155-資深工程師審查報告.md`，本次更新將 Docker image build 邊界升級為正式 Docker build contract gate，版本標記為 `v1.0.71`。

## 更新項目

1. 新增 `scripts/check-docker-build-contract.mjs`，固定 Dockerfile、`CGO_ENABLED=0`、`api-worker` / `migrate` binaries、`distroless/static-debian12` runtime image、migration copy、entrypoint、Makefile、root CI 與 standalone workflow build tags。
2. `production-api-worker/Makefile` 新增 `docker-build-check`，先跑 static gate，再執行 `docker build -t production-api-worker:local .`。
3. `.github/workflows/ci.yml` 新增 `Check Docker build contract`。
4. README、production README、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、第 7 / 9 / 11 章、`Cheatsheet/cheatsheet-advanced.md` 與 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步新增 Docker build contract gate 入口。
5. Contract gate inventory 從 39 個 root checker 更新為 40 個。
6. `VERSION`、`CHANGELOG.md`、version-sensitive contract checks 與 release artifact chain 更新到 `v1.0.71`。
7. 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`，並保留 GitHub Pages link fix。

## 驗證要求

- `node --check scripts/check-docker-build-contract.mjs`
- `node scripts/check-docker-build-contract.mjs`
- 全部 root `scripts/check-*-contract.mjs`
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML parse
- `cd production-api-worker && docker compose config`
- 可行時執行 `cd production-api-worker && make docker-build-check`
- `git diff --check`
