# 內容需要更新的部分

| 項目 | 內容 |
|---|---|
| 日期 | 2026-05-17 |
| 完整日期時間 | 2026-05-17 10:31:08 CST (+0800) |
| 來源審查報告 | `審查報告/2026-05-17-103108-資深工程師審查報告.md` |
| 目標版本 | v1.0.29 |

## 必要更新

| 優先級 | 更新項目 | 修改內容 | 驗證方式 |
|---|---|---|---|
| P1 | 語法應用 SVG 流程圖品質門檻 | 新增 `scripts/check-syntax-flow-svg.mjs`，固定 25 個單語法 flow、25 個 blueprint、標準流程圖符號與 SVG metadata | `node scripts/check-syntax-flow-svg.mjs` |
| P1 | 補充頁發布紀錄 | 將 `docs/golang-syntax-application-svg.html` 與 `圖解筆記3-4整合/golang-syntax-application-svg.html` 的標準流程圖改版納入 v1.0.29 changelog | `rg -n "v1.0.29|check-syntax-flow-svg|標準流程圖" CHANGELOG.md README.md` |
| P1 | README release gate | 在版本策略與驗證矩陣加入語法 SVG 流程圖要求 | `rg -n "語法 SVG 流程圖|check-syntax-flow-svg" README.md` |
| P1 | docs index 同步 | 將整合課程 HTML 複製到 `docs/index.html`，再套用 GitHub Pages link fix | `node scripts/fix-docs-index-links.mjs --sync-source && node scripts/fix-docs-index-links.mjs --check` |
| P1 | 版本標註 | `VERSION` bump 到 `v1.0.29`，`CHANGELOG.md` 新增本輪修改內容 | `cat VERSION && rg -n "## v1.0.29" CHANGELOG.md` |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| production API 行為修改 | v1.0.28 已完成 security contract，本輪只做教程視覺品質 gate |
| Go ReleaseNote 重產 | 官方 Go patch 狀態未出現新差異，本輪只引用官方 release history 作基準 |
| 未追蹤截圖與 `.vscode/settings.json` | 屬於既有本機檔，不納入本輪版本交付 |

## 驗收條件

1. 新增審查報告、內容更新清單、更新資料三個時間戳 Markdown 檔。
2. `README.md`、`VERSION`、`CHANGELOG.md` 對齊 `v1.0.29`。
3. 語法 SVG 檢查腳本通過，且同時覆蓋 `docs/` 與整合來源。
4. `docs/index.html` 由整合課程同步並通過 link fix check。
5. 建立 annotated tag `v1.0.29` 並推送到 GitHub；若網路失敗，需記錄實際錯誤。
