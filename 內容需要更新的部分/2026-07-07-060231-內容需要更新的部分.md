# 2026-07-07 06:02:31 CST +0800 內容需要更新的部分

## 更新結論

依據 `審查報告/2026-07-07-060231-資深工程師審查報告.md`，本輪需將「Prometheus rule 尚未接入正式 Alertmanager routing / receiver / escalation governance」升級為正式 `Alertmanager routing governance contract gate`，版本標記為 `v1.0.81`。

## 修改清單

1. 新增 `configs/prometheus/alertmanager.yml`。
2. 更新 `configs/prometheus/prometheus.yml`，加入 `alerting.alertmanagers` target `alertmanager:9093`。
3. 更新 `production-api-worker/docker-compose.yml`，在 `monitoring` profile 加入 Alertmanager service、config mount 與 `9093:9093` port。
4. 新增 `scripts/check-alertmanager-routing-contract.mjs`。
5. `production-api-worker/Makefile` 新增 `alertmanager-routing-check`。
6. `.github/workflows/ci.yml` 新增 `Check Alertmanager routing governance contract`。
7. `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/operational-runbook.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml` 新增 Alertmanager routing governance contract gate 條款。
8. `chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md`、`圖解筆記3-4整合/golang-complete-visual-course.html` 新增教材入口。
9. Contract gate inventory 從 48 個 root contract checker 更新為 49 個。
10. `VERSION`、`CHANGELOG.md`、OpenAPI、API contract 與 version-sensitive contract checks 更新為 `v1.0.81`。
11. `scripts/check-release-artifact-chain-contract.mjs` 更新為 `2026-07-07-060231` artifact chain。
12. 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`。

## 驗證需求

- `node --check scripts/check-alertmanager-routing-contract.mjs`
- `node scripts/check-alertmanager-routing-contract.mjs`
- `cd production-api-worker && make alertmanager-routing-check`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/check-release-artifact-chain-contract.mjs`
- 全部 root `scripts/check-*-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML parse
- `cd production-api-worker && docker compose config`
- `git diff --check`
