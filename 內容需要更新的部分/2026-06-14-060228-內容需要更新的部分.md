# 2026-06-14 06:02:28 CST +0800 內容需要更新的部分

依據 `審查報告/2026-06-14-060228-資深工程師審查報告.md`，本輪更新目標是發布 `v1.0.58`，把 GitHub Pages / `docs/index.html` 發布同步流程升級為正式 `Docs publishing contract gate`。

## 必要更新清單

| 類別 | 需要更新 | 驗收標準 |
|---|---|---|
| 靜態 gate | 新增 `scripts/check-docs-publishing-contract.mjs` | 可檢查 docs source、`docs/index.html`、link fix、HTML home links、Makefile、CI 與教材入口 |
| Makefile | 新增 `docs-publishing-check` | `cd production-api-worker && make docs-publishing-check` 可呼叫 root checker |
| CI | 新增 `Check docs publishing contract` | GitHub Actions root-course job 會執行新 checker |
| Contract inventory | 26 個 root checker 更新為 27 個 | `node scripts/check-contract-gate-inventory-contract.mjs` 通過 |
| OpenAPI metadata | 修正 `all 25 root contract checker scripts` | OpenAPI 改為 27 個 checker 並保留 `v1.0.58` |
| 教材同步 | README、production README、API contract、第 7 / 9 / 11 章、Cheatsheet、整合視覺課程 | 都包含 Docs publishing contract gate 與 checker 指令 |
| 發版同步 | `VERSION`、`CHANGELOG.md`、version-sensitive scripts、`docs/index.html` | docs sync/check、HTML home-link check、git diff check 通過 |

## 本輪不做

- 不新增 production runtime feature。
- 不改 Go baseline；官方來源仍顯示 `go1.26.4` 為目前最新 patch。
- 不擴大到 unrelated refactor；本輪只處理 docs publishing contract 與已發現的 OpenAPI checker 數量漂移。
