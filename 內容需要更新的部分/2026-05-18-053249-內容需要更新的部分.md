# 內容需要更新的部分

| 項目 | 內容 |
|---|---|
| 產生日期 | 2026-05-18 |
| 完整日期時間 | 2026-05-18 05:32:49 CST +0800 |
| 對應審查報告 | `審查報告/2026-05-18-053249-資深工程師審查報告.md` |
| 目標版本 | v1.0.35 |

## 必須更新

| 優先級 | 更新項目 | 目的 | 驗收方式 |
|---|---|---|---|
| P1 | `api-worker` 監聽 `SIGINT` 與 `SIGTERM` | 確保 Docker / Kubernetes rolling deploy 進入 graceful shutdown | `go test ./cmd/api-worker -run 'TestMonitoredSignalsContract' -count=1` |
| P1 | 新增 `TRUSTED_PROXY_CIDRS` 設定與 CIDR 驗證 | 避免未信任來源用 `X-Forwarded-For` 偽造 rate limit key | `go test ./internal/config -count=1` |
| P1 | rate limiter 只信任 trusted proxy forwarded IP | 固定 direct client 使用 `RemoteAddr`，proxy client 才用 forwarded IP | `go test ./internal/api -run 'TestRateLimitTrustedProxyContract' -count=1` |
| P1 | 更新 rate limit / shutdown 檢查腳本與 CI | 讓 contract gate 防止文件與實作漂移 | `node scripts/check-rate-limit-contract.mjs`、`node scripts/check-shutdown-signal-contract.mjs` |
| P2 | README、API contract、runbook、章節、Cheat Sheet、整合視覺課程同步 | 讓教材深度與 production 操作邊界一致 | `node scripts/check-html-home-links.mjs` |
| P2 | `golang-complete-visual-course.html` 複製到 `docs/index.html` | 確保 GitHub Pages 首頁與來源一致 | `node scripts/fix-docs-index-links.mjs --check` |
| P2 | 標註 `v1.0.35`、commit、tag、push | 完成本輪版本化交付 | `git push origin main refs/tags/v1.0.35` |

## 不在本輪處理

| 項目 | 原因 |
|---|---|
| 真實 Kubernetes rolling update | 需外部叢集環境，本輪只固定本地可測 signal set |
| 分散式 rate limit | 需 Redis / gateway / service mesh 決策，超出最小安全修正 |
| 多代理鏈完整解析策略 | 本輪採用第一個 forwarded IP 與 trusted proxy 邊界，足以支撐教材示範 |
