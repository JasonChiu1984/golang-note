# 內容需要更新的部分

- 產出日期：2026-06-20
- 完整日期時間：2026-06-20 06:05:25 CST +0800
- 對應版本：v1.0.64
- 依據審查報告：`審查報告/2026-06-20-060525-資深工程師審查報告.md`

## 本次需要更新

1. 新增 `scripts/check-idempotency-key-contract.mjs`，固定 Idempotency key contract gate。
2. 在 `production-api-worker/internal/domain/types.go` 補 `IdempotencyKey` 與 validation，限制 key 不含空白且長度不超過 128 bytes。
3. 在 `production-api-worker/internal/app/service.go` 補 `GetJobByIdempotencyKey` transaction lookup；同一 `Idempotency-Key` 重試回既有 job，且不重複 enqueue。
4. 在 `production-api-worker/internal/repository/memory.go`、`production-api-worker/internal/repository/postgres.go` 補 memory/Postgres idempotency lookup。
5. 在 `production-api-worker/migrations/001_init.sql` 與 `002_idempotency_key.sql` 補 `idempotency_key` 欄位與 `idx_jobs_idempotency_key` unique index。
6. 在 `production-api-worker/internal/api/handler.go` 讀取 `Idempotency-Key` header，並新增 `TestIdempotencyKeyContract` / `TestCreateJobIdempotencyKeyContract`。
7. 將 `idempotency-key-check` 接入 `production-api-worker/Makefile`、`.github/workflows/ci.yml` 與 contract inventory。
8. 同步 README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、Cheatsheet、整合視覺課程與 `docs/index.html`。
9. 將版本標記更新為 `v1.0.64`，並更新 `CHANGELOG.md`。

## 驗證要求

- `node --check scripts/check-idempotency-key-contract.mjs`
- `node scripts/check-idempotency-key-contract.mjs`
- all root `scripts/check-*-contract.mjs`
- `cd production-api-worker && make idempotency-key-check`
- `cd production-api-worker && make ci-contract`
- root / production Go tests
- docs index sync/check
- `git diff --check`
