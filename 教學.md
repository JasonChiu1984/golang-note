# Go (Golang) 實用入門教程

本教程為初學者設計，涵蓋環境設定、核心語法、並發模型與開發實務，建議邊讀邊寫程式加深理解。

## 一、環境安裝與專案初始化

```bash
# 1. 安裝 Go（建議 1.20+）
# 官網：https://go.dev/dl/
go version

# 2. 建立專案並啟用模組管理
mkdir my-go-app && cd my-go-app
go mod init my-go-app

# 3. 推薦開發工具
# VS Code + Go 擴充功能（內含 gopls、dlv、go test）
# 或 GoLand / Neovim + nvim-go
```

## 二、基礎語法速覽

### 1. 變數與型別
```go
package main

import "fmt"

func main() {
    // 宣告與賦值
    var age int = 25
    name := "Alice" // 型別推論，僅用於函數內

    // 多變數賦值
    x, y := 1, 2
    x, y = y, x // 交換值

    // 常用型別：int/uint, float64, bool, string, byte/rune
    fmt.Println(age, name, x, y)
}
```

### 2. 流程控制
```go
func controlFlow() {
    // Go 只有 for 迴圈（無 while/do-while）
    for i := 0; i < 5; i++ {
        fmt.Println(i)
    }

    // if 條件可先宣告變數
    if value := 10; value > 5 {
        fmt.Println("大於 5")
    }

    // switch 自動 break，需 fallthrough 才繼續
    day := "Mon"
    switch day {
    case "Mon", "Tue", "Wed", "Thu", "Fri":
        fmt.Println("工作日")
    default:
        fmt.Println("假日")
    }

    // defer：函數回傳前執行（LIFO）
    defer fmt.Println("最後執行")
    fmt.Println("先執行")
}
```

### 3. 函數
```go
// 多回傳值是 Go 的常見模式
func divide(a, b float64) (float64, error) {
    if b == 0 {
        return 0, fmt.Errorf("除數不能為 0")
    }
    return a / b, nil
}
```

## 三、核心概念

### 1. 結構體（Struct）
```go
type User struct {
    ID    int
    Name  string
    Email string
}

func (u User) Greet() string { // 方法綁定
    return fmt.Sprintf("Hello, %s", u.Name)
}
```

### 2. 介面（Interface）
Go 介面為 **隱式實作**，不需 `implements`：
```go
type Greeter interface {
    Greet() string
}

func SayHi(g Greeter) {
    fmt.Println(g.Greet())
}

// 自動滿足 Greeter
SayHi(User{Name: "Bob"})
```

### 3. 切片與映射
```go
func sliceAndMap() {
    // 切片：底層為陣列的視圖，含 len, cap, ptr
    s := []int{1, 2, 3}
    s = append(s, 4) // 自動擴容

    // 映射
    m := map[string]int{"a": 1, "b": 2}
    m["c"] = 3

    // 安全讀取
    if v, ok := m["x"]; ok {
        fmt.Println(v)
    } else {
        fmt.Println("不存在")
    }
}
```

### 4. 並發模型（Goroutine & Channel）
```go
package main

import (
    "fmt"
    "sync"
)

func worker(id int, jobs <-chan int, results chan<- int, wg *sync.WaitGroup) {
    defer wg.Done()
    for j := range jobs {
        fmt.Printf("Worker %d 處理任務 %d\n", id, j)
        results <- j * 2
    }
}

func main() {
    jobs := make(chan int, 10)
    results := make(chan int, 10)
    var wg sync.WaitGroup

    // 啟動 3 個 worker
    for w := 1; w <= 3; w++ {
        wg.Add(1)
        go worker(w, jobs, results, &wg)
    }

    // 送資料
    for j := 1; j <= 9; j++ {
        jobs <- j
    }
    close(jobs) // 關閉通道，worker 會自動結束

    wg.Wait()
    close(results)

    // 收集結果
    for r := range results {
        fmt.Println("結果:", r)
    }
}
```

## 四、開發實務與最佳實踐

| 主題 | 建議 |
|------|------|
| **錯誤處理** | 明確檢查 `if err != nil`，勿忽略 `nil` |
| **程式碼風格** | `gofmt` + `go vet` + `staticcheck` |
| **測試** | 寫 `_test.go`，使用 `go test`，推薦 Table-Driven Tests |
| **效能優化** | 避免頻繁 `make`/`append`，使用 `sync.Pool`，注意 GC 壓力 |
| **模組依賴** | 固定版本用 `go mod tidy`，避免 `go get -u` 隨意升級 |

## 五、下一步學習路徑

1. **網路開發**：`net/http` → `gin` / `echo` / `fiber`
2. **資料庫**：`database/sql` → `gorm` / `sqlc` / `pgx`
3. **API 設計**：REST / GraphQL / gRPC + Protobuf
4. **進階並發**：`context` 取消機制、`sync.Map`、`channel` 模式（worker pool、fan-in/fan-out）
5. **部署與監控**：Docker 打包、`prometheus` + `grafana`、`otel` 追蹤

## 六、推薦資源

- 📘 官方文件：https://go.dev/doc/
- 📖 書：《Go 語言聖經》（A Tour of Go 線上互動版）
- 🛠 實作：Go by Example https://gobyexample.com/
- 💡 社群：golang-nl、Go 台灣社群、GitHub `golang/go`

---

> 💡 **提示**：需要我針對某個區塊（例如：並發模式、REST API 實作、測試撰寫、或專案結構）提供 **完整可執行的範例專案** 嗎？告訴我你的使用情境，我會為你客製化延伸內容。