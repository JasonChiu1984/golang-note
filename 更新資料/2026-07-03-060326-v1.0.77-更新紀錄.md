# v1.0.77 更新紀錄

- 更新日期：2026-07-03
- 完整日期時間：2026-07-03 06:03:26 CST +0800
- 對應審查報告：`審查報告/2026-07-03-060326-資深工程師審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-07-03-060326-內容需要更新的部分.md`
- 更新紀錄檔名：`2026-07-03-060326-v1.0.77-更新紀錄.md`
- 版本：v1.0.77

## 更新摘要

本次更新聚焦 API contract scope coverage。教程整體深度與廣度已足夠，不新增重複章節；改為補強 `production-api-worker/docs/api-contract.md` 首段適用範圍，使 API contract header 明確同步 Docs publishing contract gate 與發布面 scope。

## 修改內容

- `production-api-worker/docs/api-contract.md`
  - 版本更新為 `v1.0.77`。
  - 基準日期更新為 `2026-07-03`。
  - 首段適用範圍新增 API contract scope coverage 與 Docs publishing contract gate。
  - 新增 API contract scope coverage 說明，要求新增或調整 release gate 時同步 header、表格與 OpenAPI description。

- `scripts/check-openapi-contract.mjs`
  - 強化 OpenAPI contract checker，要求 `production-api-worker/docs/api-contract.md` 保留 API contract scope coverage、Docs publishing contract gate 與 `node scripts/check-openapi-contract.mjs`。
  - 要求 `production-api-worker/api/openapi.yaml` 保留 API contract scope coverage description。

- 教材與發布面
  - 更新 `README.md`、`production-api-worker/README.md`、`production-api-worker/api/openapi.yaml`、第 7 / 9 / 11 章、`Cheatsheet/cheatsheet-advanced.md` 與 `圖解筆記3-4整合/golang-complete-visual-course.html`。
  - 已將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製到 `docs/index.html`，並執行 `node scripts/fix-docs-index-links.mjs --sync-source`。

- Release metadata
  - `VERSION` 更新為 `v1.0.77`。
  - `CHANGELOG.md` 新增 `v1.0.77 - 2026-07-03` 條目。
  - version-sensitive contract checks 更新為 `v1.0.77`。

## 驗證紀錄

- `node scripts/check-openapi-contract.mjs`：通過。
- `node scripts/check-contract-gate-inventory-contract.mjs`：通過，維持 45 個 root contract checker。
- `node scripts/check-docs-publishing-contract.mjs`：通過，`docs/index.html` link check 與 70 個 HTML home link check 通過。
- 全部 root `scripts/check-*-contract.mjs`（排除產生本更新紀錄前的 release artifact chain check）：通過。
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`：通過。
- `cd production-api-worker && docker compose config`：通過。
- root `go test ./... -count=1` 與 production focused contract selectors 使用 repo-local cache 執行約 90 秒仍無輸出後中斷；本輪未改 Go runtime code，判定為本機 macOS Go compile-path slowness，需由 CI 或較穩定本機 Go toolchain 補 runtime confirmation。

## 發布紀錄

- `docs/index.html` 已同步。
- GitHub push：待本更新紀錄納入 release commit 後執行。
