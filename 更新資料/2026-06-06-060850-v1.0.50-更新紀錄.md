# v1.0.50 更新紀錄

> 產出日期：2026-06-06
> 完整日期時間：2026-06-06 06:08:50 CST +0800
> 版本：v1.0.50
> 主題：Trusted proxy client IP contract gate

## 更新摘要

- 依資深工程師審查報告，將既有 Trusted proxy client IP 行為升級為正式 static contract gate。
- 本輪開工前已先補推上一輪 local-only `v1.0.49`：`git push origin main refs/tags/v1.0.49` 成功，`origin/main` 從 `7cde1e4` 前進到 `da00075`，並建立遠端 tag `v1.0.49`。
- 新增 `scripts/check-trusted-proxy-contract.mjs`，固定 `TRUSTED_PROXY_CIDRS`、`X-Forwarded-For` 第一個 IP、untrusted `RemoteAddr` fallback、`TestRateLimitTrustedProxyContract`、文件、runbook、Makefile 與 CI 入口。
- 新增 `make trusted-proxy-check` 與 GitHub Actions `Check trusted proxy contract`。
- 同步 README、production README、API contract、operational runbook、第 7 / 11 章、進階 Cheat Sheet 與整合視覺課程。
- 更新 `VERSION`、`CHANGELOG.md`、OpenAPI version、API contract version 與 version-sensitive static checks 至 `v1.0.50`。
- 重新同步 `docs/index.html`。

## 官方 Go 基準

- 本輪已重新確認官方 Go Release History。
- 教材基準維持 Go 1.26.4；Go 1.25 維護線維持 Go 1.25.11。

## 驗證紀錄

- [x] `node --check scripts/check-trusted-proxy-contract.mjs`
- [x] `node scripts/check-trusted-proxy-contract.mjs`
- [x] 全部 `scripts/check-*-contract.mjs` syntax / runtime checks
- [x] `cd production-api-worker && make trusted-proxy-check`
- [x] `node scripts/check-operational-runbook.mjs`
- [x] `node scripts/check-prometheus-config.mjs`
- [x] `node scripts/check-syntax-flow-svg.mjs`
- [x] `node scripts/fix-docs-index-links.mjs --sync-source`
- [x] `node scripts/fix-docs-index-links.mjs --check`
- [x] `node scripts/check-html-home-links.mjs`
- [x] `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`
- [x] `cd production-api-worker && make ci-contract`
- [x] root `GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1`
- [x] production-api-worker `GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1`
- [x] `git diff --check`

## Git 發布紀錄

- Commit subject：`Release v1.0.50 trusted proxy contract gate`
- Annotated tag：`v1.0.50`
- Tag message：標註 Trusted proxy client IP contract gate、新增 static check、Makefile / CI 入口、文件同步與驗證摘要。
- Push 狀態：`git push origin main refs/tags/v1.0.50` 已成功，push output 顯示 `main` 從 `da00075` 前進到 release commit，並建立遠端 tag `v1.0.50`。
- Traceability 修正：push 成功後補寫本段真實發布結果，並以精確 `--force-with-lease` 更新剛建立的 automation release commit / tag，避免更新紀錄停留在 placeholder。
