# v1.0.48 更新紀錄

更新日期時間：2026-06-04 06:02:21 CST +0800

## 更新摘要

本輪依據 2026-06-04 06:02:21 CST +0800 的資深工程師審查報告，將既有 readiness / liveness 行為升級為正式 Readiness lifecycle contract gate。重點是固定 `/livez=200`、`/readyz=200`、draining 時 `/readyz=503`、health probes public、Go contract tests、OpenAPI、Makefile 與 GitHub Actions。

## 修改內容

| 類別 | 內容 |
|---|---|
| 審查報告 | 新增 `審查報告/2026-06-04-060221-資深工程師審查報告.md` |
| 更新清單 | 新增 `內容需要更新的部分/2026-06-04-060221-內容需要更新的部分.md` |
| Static gate | 新增 `scripts/check-readiness-contract.mjs` |
| Makefile | 新增 `readiness-check` target |
| CI | 新增 `Check readiness contract` GitHub Actions step |
| 文件 | README、production README、API contract、OpenAPI、第 7 / 11 章與整合視覺課程加入 Readiness lifecycle contract gate |
| Pages | 將 `golang-complete-visual-course.html` 同步為 `docs/index.html` 並套用 Pages link fix |
| 版本 | `VERSION`、`CHANGELOG.md`、OpenAPI 與 version-sensitive contract checks 更新到 `v1.0.48` |

## 官方 Go 基準

查核日期時間：2026-06-04 06:02:21 CST +0800

| 線別 | 版本 | 發布日期 |
|---|---|---:|
| stable | Go 1.26.4 | 2026-06-02 |
| maintained previous | Go 1.25.11 | 2026-06-02 |

## 驗證紀錄

| 指令 | 結果 |
|---|---|
| `node --check scripts/check-readiness-contract.mjs` | 通過 |
| `node scripts/check-readiness-contract.mjs` | 通過，回傳 `contract: readiness lifecycle` |
| `node --check scripts/check-openapi-contract.mjs && node scripts/check-openapi-contract.mjs` | 通過 |
| `for f in scripts/check-*.mjs; do node --check "$f" && node "$f"; done` | 通過全部 root Node contract gates |
| `node scripts/fix-docs-index-links.mjs --sync-source && node scripts/fix-docs-index-links.mjs --check` | 通過，`docs/index.html` 已由整合課程同步 |
| `node scripts/check-html-home-links.mjs` | 通過，70 pages link back to `docs/index.html` |
| `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'` | 通過 |
| `cd production-api-worker && make readiness-check` | 通過 |
| `cd production-api-worker && make ci-contract` | 通過 |
| root `go test ./... -count=1` | 通過 |
| `cd production-api-worker && go test ./... -count=1` | 通過 |
| `git diff --check` | 通過 |

## 限制與補充

- `node scripts/generate-go-release-notes.mjs` 嘗試重產時，因本地 DNS 無法解析 `go.dev` 而失敗：`getaddrinfo ENOTFOUND go.dev`。
- 已依本輪查核到的官方 Go Release History 手動同步 Go 1.26.4 / Go 1.25.11 patch 狀態到 README、ReleaseNote generator 與已生成 HTML。
