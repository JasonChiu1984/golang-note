# v1.0.49 更新紀錄

> 產出日期：2026-06-05  
> 完整日期時間：2026-06-05 06:49:32 CST +0800  
> 版本：v1.0.49  
> 主題：DB pool contract gate

## 更新摘要

- 依資深工程師審查報告，將既有 DB pool 行為升級為正式 static contract gate。
- 新增 `scripts/check-db-pool-contract.mjs`，固定 DB pool env、config default / override / fail-fast、repository pool 套用、`openStore` 傳遞 config、文件、Makefile 與 CI 入口。
- 新增 `make db-pool-check` 與 GitHub Actions `Check DB pool contract`。
- 同步 README、production README、API contract、第 7 / 11 章、進階 Cheat Sheet 與整合視覺課程。
- 更新 `VERSION`、`CHANGELOG.md`、OpenAPI version、API contract version 與 version-sensitive static checks 至 `v1.0.49`。
- 重新同步 `docs/index.html`。

## 官方 Go 基準

- 本輪已重新確認官方 Go Release History。
- 教材基準維持 Go 1.26.4；Go 1.25 維護線維持 Go 1.25.11。

## 驗證紀錄

- [x] `node --check scripts/check-db-pool-contract.mjs`
- [x] `node scripts/check-db-pool-contract.mjs`
- [x] `cd production-api-worker && make db-pool-check`
- [x] `node scripts/check-openapi-contract.mjs`
- [x] 全部 `scripts/check-*-contract.mjs` runtime checks
- [x] `node scripts/check-operational-runbook.mjs`
- [x] `node scripts/check-prometheus-config.mjs`
- [x] `node scripts/check-syntax-flow-svg.mjs`
- [x] `node scripts/fix-docs-index-links.mjs --sync-source`
- [x] `node scripts/fix-docs-index-links.mjs --check`
- [x] `node scripts/check-html-home-links.mjs`
- [x] `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`
- [x] `cd production-api-worker && make ci-contract`
- [x] root `GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1`
- [x] production-api-worker `GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1`
- [x] `git diff --check`

## Git 發布紀錄

- Commit subject：`Release v1.0.49 db pool contract gate`
- Annotated tag：`v1.0.49`
- Tag message：標註 DB pool contract gate、新增 static check、Makefile / CI 入口、文件同步與驗證摘要。
- Push 狀態：本地 commit / tag 已完成；`git push origin main refs/tags/v1.0.49` 連續三次因 DNS 無法解析 `github.com` 失敗，錯誤為 `Could not resolve host: github.com`。
- 續推指令：網路恢復後執行 `git push origin main refs/tags/v1.0.49`。
