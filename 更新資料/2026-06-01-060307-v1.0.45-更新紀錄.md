# v1.0.45 更新紀錄

- 更新日期：2026-06-01
- 完整時間：2026-06-01 06:03:07 CST +0800
- 更新主題：Migration contract gate

## 更新摘要

本次依據資深工程師審查報告，將已存在於教材與 production-api-worker 的 Migration contract 正式收斂成可重跑靜態 gate。更新重點是讓 `DATABASE_URL`、`MIGRATIONS_DIR`、`MIGRATION_TIMEOUT`、`schema_migrations`、transaction apply、Go tests、Makefile、GitHub Actions 與教學文件保持一致。

## 修改項目

| 類別 | 檔案 / 內容 |
|---|---|
| Static gate | 新增 `scripts/check-migration-contract.mjs` |
| Build tooling | `production-api-worker/Makefile` 新增 `migration-check` |
| CI | `.github/workflows/ci.yml` 新增 `Check migration contract` |
| 文件 | 更新 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md` |
| 教學內容 | 更新第 7 章、第 11 章、進階 Cheat Sheet、整合視覺課程 |
| OpenAPI / version | 更新 `production-api-worker/api/openapi.yaml`、`VERSION`、`CHANGELOG.md` 與 version-sensitive checks |
| Pages | 將整合視覺課程同步到 `docs/index.html` |

## 驗證紀錄

| 指令 | 結果 |
|---|---|
| `node --check scripts/check-migration-contract.mjs` | 通過 |
| `node scripts/check-migration-contract.mjs` | 通過，回報 `contract: migration`、`files: 15` |
| 既有 version-sensitive contract checks | 通過：OpenAPI、queue backpressure、retry cancellation、worker failure、API security、request correlation、CORS、rate limit、request body limit |
| 其他 production static gates | 通過：HTTP timeout、shutdown signal、OTLP collector、pprof、operational runbook、Prometheus config、HTML home links |
| `node scripts/fix-docs-index-links.mjs --sync-source && node scripts/fix-docs-index-links.mjs --check` | 通過，`docs/index.html` 已同步 |
| `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'` | 通過 |
| `cd production-api-worker && GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' make migration-check` | 通過 |
| `cd production-api-worker && GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' make ci-contract` | 通過 |
| `TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1` | root module 通過 |
| `cd production-api-worker && TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1` | production-api-worker 通過 |
| `git diff --check` | 通過 |
