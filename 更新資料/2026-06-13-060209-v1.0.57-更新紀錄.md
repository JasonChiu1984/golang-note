# 2026-06-13 06:02:09 CST +0800 v1.0.57 更新紀錄

## 發布摘要

本次發布 `v1.0.57`，主題為 Operational observability contract gate。這輪依據資深工程師審查結果，沒有新增 runtime feature，而是把既有 Operational runbook 與 Prometheus config 檢查升級為正式 `check-*-contract.mjs` release gate，讓 incident workflow、Prometheus scrape config、alert rules、Compose monitoring profile 與 API key scrape auth 風險都進入 Makefile、GitHub Actions、contract inventory 與教材入口。

## 修改內容

| 類型 | 檔案 / 區域 | 說明 |
|---|---|---|
| 審查報告 | `審查報告/2026-06-13-060209-資深工程師審查報告.md` | 確認目前深度廣度足夠，缺口收斂為 observability gate traceability |
| 更新清單 | `內容需要更新的部分/2026-06-13-060209-內容需要更新的部分.md` | 明列新 checker、Makefile、CI、inventory、文件與版本同步項目 |
| 新 static gate | `scripts/check-operational-observability-contract.mjs` | 固定 runbook、Prometheus config、alert rules、Compose monitoring profile、API key scrape auth 風險、Makefile 與 CI 入口 |
| Makefile / CI | `production-api-worker/Makefile`、`.github/workflows/ci.yml` | 新增 `operational-observability-check` 與 `Check operational observability contract` |
| Contract inventory | `scripts/check-contract-gate-inventory-contract.mjs` | root contract checker 數量由 25 更新為 26 |
| 教材同步 | `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程 | 加入 Operational observability contract gate，並同步 `v1.0.57` |
| docs 首頁 | `docs/index.html` | 已由 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製後執行 GitHub Pages link fix |
| 版本紀錄 | `VERSION`、`CHANGELOG.md` | 更新為 `v1.0.57` |

## 驗證紀錄

| 指令 | 結果 |
|---|---|
| 官方 Go source check：`https://go.dev/VERSION?m=text`、`https://go.dev/doc/devel/release` | 確認最新 baseline 仍為 `go1.26.4`，Go Release History 列 `go1.26.4` / `go1.25.11` 於 2026-06-02 發布 |
| `node --check scripts/check-operational-observability-contract.mjs` | 通過 |
| `node scripts/check-operational-observability-contract.mjs` | 通過，14 files |
| `cd production-api-worker && make operational-observability-check && make runbook-check && make prometheus-check && make contract-gate-inventory-check` | 通過 |
| `ruby -e 'require "yaml"; ...'` 解析 CI、OpenAPI、Prometheus config、alert rules | 通過 |
| `cp 圖解筆記3-4整合/golang-complete-visual-course.html docs/index.html && node scripts/fix-docs-index-links.mjs --sync-source` | 通過，`docs/index.html` 已同步 |
| `node scripts/fix-docs-index-links.mjs --check` | 通過 |
| `node scripts/check-html-home-links.mjs` | 通過，70 pages |
| `for script in scripts/check-*-contract.mjs; do node --check "$script" && node "$script"; done` | 通過，26 個 root contract checker |
| root `go test ./... -count=1` with repo-local cache | 通過 |
| `cd production-api-worker && make ci-contract` with repo-local cache | 通過 |
| `cd production-api-worker && go test ./... -count=1` with repo-local cache | 通過 |
| `cd production-api-worker && docker compose config` | 通過 |
| `git diff --check` | 通過 |

## 發布狀態

| 項目 | 狀態 |
|---|---|
| 目標版本 | `v1.0.57` |
| Commit | 本檔所在 release commit：`Release v1.0.57 operational observability contract gate` |
| Annotated tag | `v1.0.57` |
| GitHub push | 初次 `git push origin main refs/tags/v1.0.57` 成功：`97243a8..06df5f9 main -> main`，`[new tag] v1.0.57 -> v1.0.57`；補入真實 push 證據後以精確 force-with-lease 更新 main，並強制更新同一個 `v1.0.57` tag |
