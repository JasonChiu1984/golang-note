# v1.0.54 更新紀錄

- 更新日期：2026-06-10
- 完整日期時間：2026-06-10 06:02:15 CST +0800
- 版本：v1.0.54
- 來源審查報告：`審查報告/2026-06-10-060215-資深工程師審查報告.md`
- 更新清單：`內容需要更新的部分/2026-06-10-060215-內容需要更新的部分.md`

## 更新摘要

本版將 CI contract parity 正式納入 release gate。`production-api-worker/Makefile` 的 `make ci-contract` 現在與 GitHub Actions `production-contract` job 使用相同 API contract test selector，並明確保留 `TestCORSAllowedOriginsContract`，避免本機驗證與 CI 驗證分歧。

## 修改內容

| 類型 | 檔案 | 說明 |
|---|---|---|
| Static gate | `scripts/check-ci-contract-parity-contract.mjs` | 新增 Makefile / GitHub Actions API selector parity 檢查 |
| Makefile | `production-api-worker/Makefile` | `ci-contract` 納入 `TestCORSAllowedOriginsContract`，新增 `ci-contract-parity-check` |
| CI | `.github/workflows/ci.yml` | 新增 `Check CI contract parity contract` |
| 文件 | README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、Cheat Sheet、整合視覺課程 | 補上 CI contract parity gate |
| 版本 | `VERSION`、`CHANGELOG.md`、version-sensitive checkers | 更新為 `v1.0.54` |

## 驗證紀錄

- `node --check scripts/check-ci-contract-parity-contract.mjs`：通過。
- `node scripts/check-ci-contract-parity-contract.mjs`：通過，回報 `contract: "ci contract parity"`。
- `cd production-api-worker && make ci-contract-parity-check`：通過。
- 全部 `scripts/check-*-contract.mjs` 語法檢查與執行：通過。
- `node scripts/fix-docs-index-links.mjs --sync-source`：已同步 `docs/index.html`。
- `node scripts/fix-docs-index-links.mjs --check`：通過。
- `node scripts/check-html-home-links.mjs`：通過，70 pages link back to `docs/index.html`。
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`：通過。
- `git diff --check`：通過。
- root `go test ./... -count=1` 搭配 repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` 與 external link mode：通過。
- `cd production-api-worker && make ci-contract` 搭配 repo-local cache 與 external link mode：通過。
- `cd production-api-worker && go test ./... -count=1` 搭配 repo-local cache 與 external link mode：通過。
- `cd production-api-worker && docker compose config`：通過。

## GitHub 推送

- 初次 `git push origin main refs/tags/v1.0.54`：成功，GitHub 回報 `1b00fbd..36fd92f  main -> main` 與 `[new tag] v1.0.54 -> v1.0.54`。
- 初次推送後本地對齊證據：`HEAD`、`origin/main`、`v1.0.54^{}` 均為 `36fd92f9cd5e0c00ffb170756aa84a1645c41c90`。
- 回填本檔後已用 `git push --force-with-lease=main:36fd92f9cd5e0c00ffb170756aa84a1645c41c90 origin main refs/tags/v1.0.54 --force` 精準替換本輪自動化 commit 與 tag；GitHub 回報 `36fd92f...4a1424d main -> main (forced update)` 與 `v1.0.54 -> v1.0.54 (forced update)`。
- 本檔為最終 release record 的一部分；最終 `HEAD`、`origin/main`、`v1.0.54^{}` 對齊狀態以本輪結束前的 `git rev-parse` 實測為準。
