# 2026-06-27 06:01:55 CST +0800 v1.0.71 更新紀錄

## 更新摘要

本次更新依據同 timestamp 審查報告與內容更新清單，將 Docker image build 邊界升級為正式 Docker build contract gate。這個 release 不新增 runtime feature，而是把既有 Dockerfile、Makefile、root CI 與 standalone production workflow 的 build surface 固定成可被靜態驗證的 release contract。

## 修改內容

- 新增 `scripts/check-docker-build-contract.mjs`。
- `production-api-worker/Makefile` 新增 `docker-build-check`。
- `.github/workflows/ci.yml` 新增 `Check Docker build contract`。
- README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、進階 Cheat Sheet 與整合視覺課程新增 Docker build contract gate。
- Contract gate inventory 從 39 個 root contract checker 更新為 40 個。
- `VERSION`、`CHANGELOG.md` 與 version-sensitive contract checks 更新為 `v1.0.71`。
- `docs/index.html` 已依步驟四由 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步，並保留 Pages link fix。

## Artifact chain

- `審查報告/2026-06-27-060155-資深工程師審查報告.md`
- `內容需要更新的部分/2026-06-27-060155-內容需要更新的部分.md`
- `更新資料/2026-06-27-060155-v1.0.71-更新紀錄.md`

## 驗證紀錄

- `node --check scripts/check-docker-build-contract.mjs`：通過。
- `node scripts/check-docker-build-contract.mjs`：通過。
- `node scripts/check-contract-gate-inventory-contract.mjs`：通過，確認 40 個 root contract checker。
- `node scripts/check-release-artifact-chain-contract.mjs`：通過，確認 `v1.0.71` 與 `2026-06-27-060155` artifact chain。
- 全部 root `scripts/check-*-contract.mjs`：通過。
- `node scripts/fix-docs-index-links.mjs --sync-source`：已同步 `docs/index.html`。
- `node scripts/fix-docs-index-links.mjs --check`：通過。
- `node scripts/check-html-home-links.mjs`：通過，70 個頁面可回到 `docs/index.html`。
- GitHub Actions YAML parse：通過。
- `cd production-api-worker && docker compose config`：通過。
- root `go test ./... -count=1` 搭配 repo-local cache 與 external link mode：通過。
- `production-api-worker` focused config / migration tests：通過。
- `production-api-worker` focused app selector：通過。
- `production-api-worker` focused worker selector：通過。
- `git diff --check`：通過。

## 環境限制

- `cd production-api-worker && make docker-build-check` 的 static checker 已通過，但實際 `docker build -t production-api-worker:local .` 因本機 Docker daemon 未啟動而失敗：`failed to connect to the docker API at unix:///Users/jasonchiu/.docker/run/docker.sock`。
- `production-api-worker` focused API selector 與 `cmd/api-worker` selector 在本機超過約 90 秒仍無輸出後中止；此現象與前幾輪 production API 編譯路徑偏慢一致，本次 release 以 static gate wiring、root tests、config/migration/app/worker focused tests 與 CI 後續驗證作為主要證據。

## GitHub push

待完成。
