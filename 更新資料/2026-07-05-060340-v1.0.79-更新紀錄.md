# v1.0.79 更新紀錄

- 更新日期：2026-07-05
- 完整日期時間：2026-07-05 06:03:40 CST +0800
- 版本：v1.0.79
- 主題：Platform promotion policy contract gate

## 對應 artifact

- 審查報告：`審查報告/2026-07-05-060340-資深工程師審查報告.md`
- 內容需要更新的部分：`內容需要更新的部分/2026-07-05-060340-內容需要更新的部分.md`
- 更新資料：`更新資料/2026-07-05-060340-v1.0.79-更新紀錄.md`

## 更新內容

- 新增 `scripts/check-platform-promotion-policy-contract.mjs`，固定 Platform promotion policy contract gate。
- 新增 `production-api-worker/Makefile` 的 `platform-promotion-policy-check`。
- 新增 `.github/workflows/ci.yml` 的 `Check platform promotion policy contract`。
- 同步 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/operational-runbook.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、`chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md` 與 `圖解筆記3-4整合/golang-complete-visual-course.html`。
- 將 Contract gate inventory 從 46 個 root contract checker 更新為 47 個。
- 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製並同步為 `docs/index.html`。
- 將 `VERSION`、`CHANGELOG.md`、version-sensitive contract checks、API contract 與 OpenAPI 版本標記更新為 `v1.0.79`。

## 驗證紀錄

- `node --check scripts/check-platform-promotion-policy-contract.mjs`：通過。
- `node scripts/check-platform-promotion-policy-contract.mjs`：通過。
- `cd production-api-worker && make platform-promotion-policy-check`：通過。
- `node scripts/check-contract-gate-inventory-contract.mjs`：通過，確認 47 個 root contract checker。
- `node scripts/fix-docs-index-links.mjs --sync-source`：通過並同步 `docs/index.html`。
- `node scripts/fix-docs-index-links.mjs --check`：通過。
- 全部 root `scripts/check-*-contract.mjs`（排除本檔建立前必然失敗的 release artifact chain）已通過。
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'`：通過。
- `cd production-api-worker && docker compose config`：通過。
- `git diff --check`：通過。
- `go test ./... -count=1`：本機 macOS Go compile path 約 90 秒無輸出後中斷，未出現測試失敗輸出。
- `cd production-api-worker && go test ./internal/config ./internal/migration -count=1`：本機 macOS Go compile path 約 90 秒無輸出後中斷，未出現測試失敗輸出。

## 發布狀態

- GitHub push：成功。
- Push output：`dfc0d0e..35715f9 main -> main`，`[new tag] v1.0.79 -> v1.0.79`。
- 發布後需因本檔回填實際 push 結果而重建 final commit / tag，最終 refs 以本檔後續 commit 為準。
- `docs/index.html`：已由 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步。
