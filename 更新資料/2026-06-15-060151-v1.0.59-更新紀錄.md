# v1.0.59 更新紀錄

- 更新日期：2026-06-15
- 完整更新時間：2026-06-15 06:01:51 CST +0800
- 對應審查報告：`審查報告/2026-06-15-060151-資深工程師審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-06-15-060151-內容需要更新的部分.md`
- 版本：v1.0.59

## 更新摘要

本次更新將 tracked standalone workflow `production-api-worker/.github/workflows/production-api-worker.yml` 升級為 Production workflow contract gate，避免 production worker 未來抽成獨立 repo 或複製 workflow 時只保留 `go test` / build，而漏掉 production release gate。

## 修改內容

- 新增 `scripts/check-production-workflow-contract.mjs`。
- `production-api-worker/.github/workflows/production-api-worker.yml` 補齊：
  - `go mod download`
  - `go mod verify`
  - `make ci-contract`
  - `go test -race -cover ./... -count=1`
  - `go build ./cmd/api-worker`
  - `go build ./cmd/migrate`
  - `govulncheck ./...`
  - `docker build -t production-api-worker:standalone .`
  - `docker compose up -d --build`
  - `./scripts/compose-smoke.sh`
  - `docker compose logs --no-color`
  - `docker compose down -v`
- `production-api-worker/Makefile` 新增 `production-workflow-check`。
- `.github/workflows/ci.yml` 新增 `Check production workflow contract`。
- Contract gate inventory 從 27 個 root contract checker 更新為 28 個。
- `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、第 7 / 9 / 11 章、進階 Cheat Sheet 與整合視覺課程同步新增 Production workflow contract gate。
- `VERSION`、`CHANGELOG.md`、OpenAPI 與 version-sensitive contract scripts 更新為 `v1.0.59`。

## 已完成驗證

- `node --check scripts/check-production-workflow-contract.mjs`
- `node scripts/check-production-workflow-contract.mjs`
- `cd production-api-worker && make production-workflow-check`
- 全部 28 個 root `scripts/check-*-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --sync-source`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- GitHub Actions YAML / OpenAPI YAML / Prometheus YAML parse
- `git diff --check`
- root `go test ./... -count=1`，使用 repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` 與 `GOFLAGS='-ldflags=-linkmode=external'`
- `cd production-api-worker && make ci-contract`，使用 repo-local cache 與 external link mode
- `cd production-api-worker && go test ./... -count=1`，使用 repo-local cache 與 external link mode
- `cd production-api-worker && docker compose config`

## GitHub 推送狀態

- 初次 release commit：`34b671a79e2b020238a3c9c5a36d21119ef73bd3`。
- 初次 annotated tag：`v1.0.59` 指向 `34b671a79e2b020238a3c9c5a36d21119ef73bd3`。
- 初次推送結果：`git push origin main refs/tags/v1.0.59` 成功，`main` 從 `831167e` 推進到 `34b671a`，並建立新 tag `v1.0.59`。
- 補入初次推送證據後 amended commit：`8f842a432b4b90a5972e88912ec373dbeca10ce7`，tag object：`0d6d3ef311f4b3769942fb0310c678a1ab5ff56f`。
- follow-up 推送結果：
  - `git push --force-with-lease=main:34b671a79e2b020238a3c9c5a36d21119ef73bd3 origin main` 成功，`main` 從 `34b671a` 更新到 `8f842a4`。
  - `git push --force origin refs/tags/v1.0.59` 成功，tag object 從 `64fd646` 更新到 `0d6d3ef`。
- 最終發布狀態：本檔所在 release commit 經最後一次 amend 後，需以精準 `--force-with-lease` 更新 `main`，並 force 更新 `v1.0.59` annotated tag 到同一個 release commit；最終以成功 push output 與本地 `HEAD` / `origin/main` / `v1.0.59^{}` 對齊作為發布證據。
