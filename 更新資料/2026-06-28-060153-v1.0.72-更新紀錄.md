# v1.0.72 更新紀錄

- 更新日期：2026-06-28
- 完整日期時間：2026-06-28 06:01:53 CST +0800
- 版本：v1.0.72
- 對應審查報告：`審查報告/2026-06-28-060153-資深工程師審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-06-28-060153-內容需要更新的部分.md`
- 本更新紀錄：`更新資料/2026-06-28-060153-v1.0.72-更新紀錄.md`

## 更新摘要

本次發布 Compose runtime env contract gate，將 `production-api-worker/docker-compose.yml` 既有的 Postgres、migration、API、OTEL collector、Prometheus monitoring profile、runtime env 與 service dependency 正式納入 root static checker。這次不新增 runtime feature，而是把 Docker build 與 Compose smoke 之間的部署設定面補成可驗證合約。

## 修改內容

- 新增 `scripts/check-compose-runtime-env-contract.mjs`。
- `production-api-worker/Makefile` 新增 `compose-runtime-env-check`。
- `.github/workflows/ci.yml` 新增 `Check Compose runtime env contract`。
- `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml` 新增 Compose runtime env contract gate 說明。
- `chapters/07-large-project-concurrent-crawler.md`、`chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md`、`圖解筆記3-4整合/golang-complete-visual-course.html` 同步新增教材入口。
- `docs/index.html` 已由 `圖解筆記3-4整合/golang-complete-visual-course.html` 重新同步並套用 GitHub Pages link fix。
- `VERSION`、`CHANGELOG.md`、version-sensitive contract checks 更新至 `v1.0.72`。
- Contract gate inventory 從 40 個 root contract checker 更新為 41 個。

## 驗證紀錄

- `node --check scripts/check-compose-runtime-env-contract.mjs`：通過。
- `node scripts/check-compose-runtime-env-contract.mjs`：通過。
- `cd production-api-worker && make compose-runtime-env-check`：通過。
- 全 root `scripts/check-*-contract.mjs`，排除尚待本更新紀錄落地的 release artifact chain：通過。
- `node scripts/check-contract-gate-inventory-contract.mjs`：通過，確認 41 個 root contract checker。
- `node scripts/fix-docs-index-links.mjs --sync-source`：完成同步。
- `node scripts/fix-docs-index-links.mjs --check`：通過。
- `node scripts/check-html-home-links.mjs`：通過。
- `.github/workflows/ci.yml`、`production-api-worker/.github/workflows/production-api-worker.yml`、`production-api-worker/api/openapi.yaml` YAML parse：通過。
- `cd production-api-worker && docker compose config`：通過。
- root `go test ./... -count=1`，搭配 repo-local cache 與 `GOFLAGS='-ldflags=-linkmode=external'`：通過。
- `cd production-api-worker && go test ./internal/config ./internal/migration -count=1`，搭配 repo-local cache 與 `GOFLAGS='-ldflags=-linkmode=external'`：通過。
- `git diff --check`：通過。

## 本機限制

- `cd production-api-worker && go test ./... -count=1 -timeout=120s` 在本機 macOS production compile path 連續約 90 秒無輸出後中止。此現象與前幾輪 production API / shutdown / Docker static-gate 發版的本機編譯路徑偏慢一致；本輪以新增 static checker、全 root contract sweep、Compose config、root Go tests 與 focused production config/migration tests 作為本機驗證證據。

## GitHub 推送狀態

- GitHub push：remote push blocked。
- `git push origin main refs/tags/v1.0.72`：失敗，原因為 DNS 無法解析 `github.com`。
- local release complete：`HEAD` 與 `v1.0.72^{}` 已在本更新紀錄提交後以 `git rev-parse HEAD 'v1.0.72^{}'` 驗證對齊。
- remote baseline：`origin/main` 仍停在 `dc1707cedc28a5543e715925531e51ccf111f0e4`。
- ahead 狀態：本地 `main` ahead `origin/main` 2 commits。
- docs/index.html：已同步。
