# v1.0.78 更新紀錄

> 更新日期：2026-07-04
> 完整日期時間：2026-07-04 06:03:01 CST +0800
> 版本：v1.0.78
> 對應審查報告：`審查報告/2026-07-04-060301-資深工程師審查報告.md`
> 對應更新清單：`內容需要更新的部分/2026-07-04-060301-內容需要更新的部分.md`
> 更新紀錄檔案：`更新資料/2026-07-04-060301-v1.0.78-更新紀錄.md`

## 1. 更新主題

本次更新聚焦 Operational runbook scope freshness。教程整體深度與廣度已符合專案開發需求；本輪不新增 runtime feature，而是將 `production-api-worker/docs/operational-runbook.md` 的文件日期、完整日期時間、版本與近期 release governance 適用範圍升級成 root contract checker 可驗證的正式 gate。

## 2. 修改內容

- 新增 `scripts/check-operational-runbook-scope-contract.mjs`
  - 固定 Operational runbook scope freshness contract gate。
  - 要求 runbook metadata、API contract scope coverage、Docs publishing contract gate、Release artifact chain contract gate、Secret handling governance contract gate 與 Supply chain artifact governance contract gate 同步存在。
- 更新 `production-api-worker/docs/operational-runbook.md`
  - 文件日期更新為 2026-07-04。
  - 完整日期時間更新為 2026-07-04 06:03:01 CST +0800。
  - 新增版本 `v1.0.78`。
  - 首段適用範圍補入 API contract scope coverage、Docs publishing、Release artifact chain、Dependency governance、Secret handling governance 與 Supply chain artifact governance。
- 更新 Makefile 與 CI
  - `production-api-worker/Makefile` 新增 `operational-runbook-scope-check`。
  - `.github/workflows/ci.yml` 新增 `Check operational runbook scope contract`。
- 同步教材入口
  - `README.md`
  - `production-api-worker/README.md`
  - `production-api-worker/docs/api-contract.md`
  - `production-api-worker/api/openapi.yaml`
  - `chapters/07-large-project-concurrent-crawler.md`
  - `chapters/09-build-and-deploy.md`
  - `chapters/11-advanced-testing.md`
  - `Cheatsheet/cheatsheet-advanced.md`
  - `圖解筆記3-4整合/golang-complete-visual-course.html`
- Contract gate inventory
  - root contract checker inventory 從 45 個更新為 46 個。
  - version-sensitive contract checks 更新到 `v1.0.78`。
- GitHub Pages 首頁
  - `docs/index.html` 依步驟四由 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步。

## 3. 驗證紀錄

已完成：

```bash
node --check scripts/check-operational-runbook-scope-contract.mjs
node scripts/check-operational-runbook-scope-contract.mjs
cd production-api-worker && make operational-runbook-scope-check
node scripts/check-contract-gate-inventory-contract.mjs
for f in scripts/check-*-contract.mjs; do node "$f"; done
node scripts/check-release-artifact-chain-contract.mjs
node scripts/fix-docs-index-links.mjs --sync-source
node scripts/fix-docs-index-links.mjs --check
node scripts/check-html-home-links.mjs
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'
cd production-api-worker && docker compose config
git diff --check -- README.md CHANGELOG.md VERSION production-api-worker/README.md production-api-worker/docs/api-contract.md production-api-worker/docs/operational-runbook.md production-api-worker/api/openapi.yaml production-api-worker/Makefile .github/workflows/ci.yml scripts/check-operational-runbook-scope-contract.mjs scripts/check-contract-gate-inventory-contract.mjs scripts/check-release-artifact-chain-contract.mjs chapters/07-large-project-concurrent-crawler.md chapters/09-build-and-deploy.md chapters/11-advanced-testing.md Cheatsheet/cheatsheet-advanced.md 審查報告/2026-07-04-060301-資深工程師審查報告.md 內容需要更新的部分/2026-07-04-060301-內容需要更新的部分.md 更新資料/2026-07-04-060301-v1.0.78-更新紀錄.md
```

本機限制：

- root `go test ./... -count=1` 使用 repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` 與 `GOFLAGS='-ldflags=-linkmode=external'` 執行約 2 分鐘仍無 stdout/stderr，已中止；未產生 Go test failure output。
- `production-api-worker` focused Go selectors 使用 repo-local cache 執行約 2 分鐘仍無 stdout/stderr，已中止；未產生 Go test failure output。
- 完整 `git diff --check` 與 `git diff --name-only` 在大型 HTML diff 下長時間無輸出，已中止；已改以窄範圍 `git diff --check -- <changed release files>` 驗證並通過。

## 4. 發布狀態

- `docs/index.html`：已由 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步，並通過 GitHub Pages link fix 與 HTML home-link check。
- Local release：完成。
- Git tag：已建立 annotated tag `v1.0.78`。
- GitHub push：remote push blocked。
  - 第一次指令：`git push origin main refs/tags/v1.0.78`
  - 第一次結果：`fatal: unable to access 'https://github.com/JasonChiu1984/golang-note/': Could not resolve host: github.com`
  - 第二次指令：`git push origin main refs/tags/v1.0.78`
  - 第二次結果：`fatal: unable to access 'https://github.com/JasonChiu1984/golang-note/': Could not resolve host: github.com`
  - local `origin/main`：`dfc0d0e5e5c59f4215af7b105986bf613c385f3a`
  - 狀態：local release complete；final `HEAD` 與 `v1.0.78^{}` 需以 `git rev-parse HEAD 'v1.0.78^{}'` 驗證，local main 會 ahead `origin/main`，待 DNS / network 恢復後重跑 `git push origin main refs/tags/v1.0.78`。
