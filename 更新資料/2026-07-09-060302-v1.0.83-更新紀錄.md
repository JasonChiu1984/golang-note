# v1.0.83 更新紀錄

> 產出日期：2026-07-09
> 完整日期時間：2026-07-09 06:03:02 CST +0800
> 版本：v1.0.83
> 主題：OTLP collector scope coverage

## Artifact chain

| 類別 | 檔案 |
|---|---|
| 審查報告 | `2026-07-09-060302-資深工程師審查報告.md` |
| 內容需要更新的部分 | `2026-07-09-060302-內容需要更新的部分.md` |
| 更新資料 | `2026-07-09-060302-v1.0.83-更新紀錄.md` |

## 修改摘要

- 將教材版本從 `v1.0.82` 更新為 `v1.0.83`。
- 強化 `scripts/check-otel-collector-contract.mjs`，新增 `production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml` 與 `圖解筆記3-4整合/golang-complete-visual-course.html` 檢查。
- 更新 `production-api-worker/docs/api-contract.md`，在 scope header 與 contract table 中明確加入 `OTLP collector contract`。
- 更新 `production-api-worker/api/openapi.yaml`，補上 `production-api-worker/otel-collector.yaml`、OTLP gRPC receiver `0.0.0.0:4317`、`debug exporter`、Compose endpoint `OTEL_EXPORTER_OTLP_ENDPOINT=otel-collector:4317` 與 `check-otel-collector-contract.mjs`。
- 更新 `production-api-worker/docs/operational-runbook.md` 日期、完整日期時間、版本與適用範圍。
- 更新 `README.md`、`CHANGELOG.md`、第 7 / 10 章與整合視覺課程，將本輪發版重點標示為 OTLP collector scope coverage。
- 已將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製到 `docs/index.html`，並執行 `node scripts/fix-docs-index-links.mjs --sync-source`。

## 驗證紀錄

已完成：

- `node scripts/check-otel-collector-contract.mjs`
- `cd production-api-worker && make otel-check`
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/check-openapi-contract.mjs`
- `node scripts/check-docs-publishing-contract.mjs`
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- 全 root `scripts/check-*-contract.mjs` sweep，共 49 個 root contract checker 通過。
- Ruby YAML parse：`.github/workflows/ci.yml`、`production-api-worker/api/openapi.yaml`、`configs/prometheus/prometheus.yml`、`configs/prometheus/alertmanager.yml`。
- `cd production-api-worker && docker compose config`
- `cd production-api-worker && docker compose --profile monitoring config`
- `git diff --check`

環境限制：

- root `go test ./... -count=1` 使用 repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` 與 `GOFLAGS='-ldflags=-linkmode=external'` 執行 60 秒以上仍無輸出，且 sandbox 不允許 `pkill` / `killall` 讀取 process list；未取得通過或失敗結果，不列為 pass evidence。
- `production-api-worker` focused Go selectors 執行 60 秒無輸出後以 Ctrl-C 中斷，沒有 failure text；本輪以 full Node contract sweep、OpenAPI、docs、YAML、Compose 與 artifact chain 作為主要本機驗證證據。

## GitHub push

本輪狀態：remote v1.0.83 created / final release-record amend local-only。

- Commit：final local `HEAD`（以 `git rev-parse HEAD` 查核）
- Tag：annotated `v1.0.83`
- 第一次 push：`git push origin main refs/tags/v1.0.83`
- 結果：失敗，`Could not resolve host: github.com`
- 第二次 push：`git push origin main refs/tags/v1.0.83`
- 結果：成功，`69913c1..ce24a9a main -> main`，`[new tag] v1.0.83 -> v1.0.83`
- 發布證據回填後 amend：local final `HEAD` 與 local `v1.0.83^{}` 已對齊。
- final amend push：`git push --force-with-lease=main:ce24a9a94a8304d391b59c09be3f43e64e52252a origin main && git push --force origin refs/tags/v1.0.83`
- 結果：失敗，`Could not resolve host: github.com`
- 最終狀態：
  - Remote `main` / remote `v1.0.83` 已建立在本輪初次成功 push 的 release commit。
  - Local `HEAD` / local `v1.0.83^{}` 保留完整發布證據回填 commit。
  - 本機 `main` ahead `origin/main` 1 commit，待 DNS 恢復後需 force-with-lease 更新剛建立的 release commit 與 tag。

恢復方式：

```bash
git push --force-with-lease=main:ce24a9a94a8304d391b59c09be3f43e64e52252a origin main
git push --force origin refs/tags/v1.0.83
```
