# v1.0.53 更新紀錄

更新時間：2026-06-09 06:02:32 CST +0800

版本：v1.0.53

## 更新摘要

本輪依據 `審查報告/2026-06-09-060232-資深工程師審查報告.md` 與 `內容需要更新的部分/2026-06-09-060232-內容需要更新的部分.md`，將既有 GitHub Actions / CI release pipeline 升級為正式 `CI quality gate contract`。本輪未新增 runtime 行為，重點是固定 root module、production contracts、race/coverage、govulncheck、Docker build、Compose smoke 與 static contract checks 的文件、Makefile、CI、章節、OpenAPI 與整合視覺課程一致性。

## 修改內容

| 類別 | 內容 |
|---|---|
| Static gate | 新增 `scripts/check-ci-quality-gate-contract.mjs`，固定 CI quality gate 文件、workflow、Makefile、OpenAPI 與章節入口 |
| Makefile | `production-api-worker/Makefile` 新增 `ci-quality-gate-check` |
| CI | `.github/workflows/ci.yml` 新增 `Check CI quality gate contract` |
| API contract | `production-api-worker/docs/api-contract.md` scope 與 compatibility rules 加入 CI quality gate contract |
| OpenAPI | `production-api-worker/api/openapi.yaml` version 更新為 `v1.0.53`，description 加入 CI quality gate contract |
| 教材文件 | README、production README、第 7 / 9 / 11 章、進階 Cheat Sheet 與整合視覺課程加入 CI quality gate static gate |
| Pages 入口 | `圖解筆記3-4整合/golang-complete-visual-course.html` 已重新同步至 `docs/index.html` 並套用 GitHub Pages link fix |
| 版本紀錄 | `VERSION` 與 `CHANGELOG.md` 更新為 `v1.0.53` |

## 官方 Go 基準

本輪重新查官方 Go Release History 與 `go.dev/VERSION`，2026-06-09 發版評估仍採 Go 1.26.4 與 Go 1.25.11 作為 2026-06 的 patch baseline。

## 驗證紀錄

已通過：

```bash
node --check scripts/check-ci-quality-gate-contract.mjs
node scripts/check-ci-quality-gate-contract.mjs
cd production-api-worker && make ci-quality-gate-check
for f in scripts/check-*-contract.mjs; do node --check "$f" && node "$f"; done
node scripts/fix-docs-index-links.mjs --sync-source
node scripts/fix-docs-index-links.mjs --check
node scripts/check-html-home-links.mjs
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'
TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1
cd production-api-worker && TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' make ci-contract
cd production-api-worker && TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1
cd production-api-worker && docker compose config
git diff --check
```

補充：未帶 repo-local cache 的第一次 `cd production-api-worker && make ci-contract` 因 macOS 系統 Go cache 權限回 `operation not permitted`，已改用 repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` 重新執行並通過。

## 發布狀態

程式與文件更新已完成並通過本地驗證。已建立 release commit（`Release v1.0.53 CI quality gate contract`）與 annotated tag `v1.0.53`，並執行：

```bash
git push origin main refs/tags/v1.0.53
```

首次推送結果成功：`main` 已由 `966351f` 推進到 `8349c42`，遠端新增 tag `v1.0.53`。因本紀錄補入實際 push 結果，後續會 amend 同一個 automation commit 並以精確 `--force-with-lease=main:8349c428bafe175f7ca5c2b9a3a8fb9fb53dd2de` 更新遠端 `main` 與 tag，最終 ref 對齊以補正後驗證為準。
