# 2026-06-14 06:02:28 CST +0800 v1.0.58 更新紀錄

## 更新摘要

本次發布 `v1.0.58`，主題為 Docs publishing contract gate。這輪依據資深工程師審查結果，沒有新增 production runtime feature，而是把每輪 release 都依賴的 `docs/index.html` 同步、GitHub Pages link fix 與 HTML 主頁回鏈檢查升級為正式 `check-*-contract.mjs` release gate。

## 修改內容

| 類別 | 檔案 | 說明 |
|---|---|---|
| 審查報告 | `審查報告/2026-06-14-060228-資深工程師審查報告.md` | 確認教材深度/廣度已足夠，本輪缺口是 docs publishing gate 與 OpenAPI checker 數量漂移 |
| 更新清單 | `內容需要更新的部分/2026-06-14-060228-內容需要更新的部分.md` | 列出 docs publishing contract gate、inventory 與版本同步項目 |
| 靜態 gate | `scripts/check-docs-publishing-contract.mjs` | 新增 docs publishing contract checker |
| Makefile / CI | `production-api-worker/Makefile`、`.github/workflows/ci.yml` | 新增 `docs-publishing-check` 與 `Check docs publishing contract` |
| 教材同步 | `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/api-contract.md`、第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程 | 加入 Docs publishing contract gate |
| OpenAPI | `production-api-worker/api/openapi.yaml` | 修正 checker inventory 數量，並同步 `v1.0.58` |
| 版本紀錄 | `VERSION`、`CHANGELOG.md`、version-sensitive scripts | 更新為 `v1.0.58` |
| 發布首頁 | `docs/index.html` | 由整合視覺課程重新同步並套用 GitHub Pages link fix |

## 驗證紀錄

| 驗證項目 | 結果 |
|---|---|
| 官方 Go baseline | `go.dev/VERSION?m=text` 顯示 `go1.26.4`；Go Release History 顯示 `go1.26.4` / `go1.25.11` 於 2026-06-02 發布 |
| 新 checker syntax | `node --check scripts/check-docs-publishing-contract.mjs` 通過 |
| Docs publishing contract | `node scripts/check-docs-publishing-contract.mjs` 通過，並確認 70 個 HTML 頁面可回到 `docs/index.html` |
| Makefile gate | `cd production-api-worker && make docs-publishing-check` 通過 |
| Contract inventory | 全部 27 個 root `scripts/check-*-contract.mjs` 通過，`node scripts/check-contract-gate-inventory-contract.mjs` 回報 `scripts: 27` |
| Docs sync | `node scripts/fix-docs-index-links.mjs --sync-source` 已重建 `docs/index.html`，`--check` 通過 |
| HTML home links | `node scripts/check-html-home-links.mjs` 通過 |
| Runbook / Prometheus / SVG | `node scripts/check-operational-runbook.mjs`、`node scripts/check-prometheus-config.mjs`、`node scripts/check-syntax-flow-svg.mjs` 通過 |
| YAML parse | `.github/workflows/ci.yml`、OpenAPI、Prometheus config、Prometheus alert rules 均可由 Ruby YAML parser 解析 |
| Root Go tests | repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` 與 `GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1` 通過 |
| Production contract tests | repo-local cache 下 `cd production-api-worker && make ci-contract` 通過 |
| Production full tests | repo-local cache 下 `cd production-api-worker && go test ./... -count=1` 通過 |
| Docker Compose config | `cd production-api-worker && docker compose config` 通過 |
| Diff hygiene | `git diff --check` 通過 |

## GitHub 發布狀態

| 項目 | 狀態 |
|---|---|
| 目標版本 | `v1.0.58` |
| Commit | 本檔所在 amended release commit：`Release v1.0.58 docs publishing contract gate`；初次 release commit 為 `a5295964432973701c771a18b534388196fdb9be` |
| Annotated tag | `v1.0.58`；初次 tag object 為 `2cc61cea614d8920a6d40646af42743435b70578` |
| GitHub push | 初次 `git push origin main refs/tags/v1.0.58` 成功：`4f7551c..a529596 main -> main`，`[new tag] v1.0.58 -> v1.0.58` |
| 發布紀錄補寫 | 本檔補入真實 push 證據後，以精確 `--force-with-lease` 更新同一個 release commit，並強制更新同一個 `v1.0.58` tag；期間一次 push 因 DNS `Could not resolve host: github.com` 失敗，重試成功 |
| 最終對齊驗證 | 以 `git rev-parse HEAD origin/main 'v1.0.58^{}'` 確認三者一致 |
