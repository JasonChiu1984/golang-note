# 2026-07-07 06:02:31 CST +0800 v1.0.81 更新紀錄

## 更新結論

本輪依據 `審查報告/2026-07-07-060231-資深工程師審查報告.md` 與 `內容需要更新的部分/2026-07-07-060231-內容需要更新的部分.md`，將 Prometheus rules 尚未接入正式 Alertmanager routing / receiver / escalation governance 的缺口，升級為 `Alertmanager routing governance contract gate`。版本更新為 `v1.0.81`。

本更新紀錄檔案：`更新資料/2026-07-07-060231-v1.0.81-更新紀錄.md`。

## 修改內容

1. 新增 `configs/prometheus/alertmanager.yml`，固定 local teaching receiver、route、grouping、repeat interval、webhook placeholder、escalation owner、silence policy 與 notification evidence。
2. 更新 `configs/prometheus/prometheus.yml`，加入 `alerting.alertmanagers` target `alertmanager:9093`。
3. 更新 `production-api-worker/docker-compose.yml`，在 `monitoring` profile 加入 Alertmanager service、config mount 與 `9093:9093` port。
4. 新增 `scripts/check-alertmanager-routing-contract.mjs`，固定 Alertmanager route、receiver owner、escalation owner、silence policy、notification evidence、Prometheus alerting target、Compose service、Makefile、CI、README、OpenAPI、runbook、章節與整合視覺課程入口。
5. `production-api-worker/Makefile` 新增 `alertmanager-routing-check`。
6. `.github/workflows/ci.yml` 新增 `Check Alertmanager routing governance contract`。
7. 同步 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/operational-runbook.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、`chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md` 與 `圖解筆記3-4整合/golang-complete-visual-course.html`。
8. Contract gate inventory 從 48 個 root contract checker 更新為 49 個。
9. `VERSION`、`CHANGELOG.md`、OpenAPI、API contract 與 version-sensitive contract checks 更新為 `v1.0.81`。
10. `scripts/check-release-artifact-chain-contract.mjs` 更新為 `2026-07-07-060231` artifact chain。
11. 步驟四將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步為 `docs/index.html`。

## 驗證紀錄

- 官方 Go Release History 已於 2026-07-07 06:02:31 CST +0800 查核；教材仍維持 Go 1.26.4 / Go 1.25.11 patch baseline。
- `node --check scripts/check-alertmanager-routing-contract.mjs` 通過。
- `node scripts/check-alertmanager-routing-contract.mjs` 通過，確認 49 個 root contract checker。
- `cd production-api-worker && make alertmanager-routing-check` 通過。
- `node scripts/check-contract-gate-inventory-contract.mjs` 通過，確認 49 個 root contract checker 都進入 GitHub Actions。
- `node scripts/check-operational-runbook-scope-contract.mjs` 通過。
- `node scripts/check-openapi-contract.mjs` 通過。
- `node scripts/check-prometheus-config-contract.mjs` 通過。
- `node scripts/check-operational-observability-contract.mjs` 通過。
- GitHub Actions、Prometheus、Alertmanager 與 Prometheus alert rules YAML parse 通過。
- `node scripts/check-release-artifact-chain-contract.mjs` 通過，artifact timestamp 為 `2026-07-07-060231`。
- 全部 root `scripts/check-*-contract.mjs` sweep 通過，共 49 個 root contract checker。
- `node scripts/fix-docs-index-links.mjs --sync-source` 已將 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步到 `docs/index.html`，並修正 GitHub Pages links。
- `node scripts/fix-docs-index-links.mjs --check` 通過。
- `node scripts/check-html-home-links.mjs` 通過，70 pages link back to `docs/index.html`。
- `cd production-api-worker && docker compose config` 通過。
- `cd production-api-worker && docker compose --profile monitoring config` 通過，確認 Alertmanager service、config mount 與 `9093:9093` 可解析。
- `git diff --check` 通過。
- `cd production-api-worker && TMPDIR="$PWD/../.tmp" GOCACHE="$PWD/../.gocache" GOMODCACHE="$PWD/../.gomodcache" GOFLAGS='-ldflags=-linkmode=external' go test ./internal/config ./internal/migration ./internal/api ./cmd/api-worker ./internal/observability ./internal/app ./internal/worker -count=1` 未完成，原因是本機 DNS 無法解析 `proxy.golang.org`，依賴無法下載；未產生 Go 測試失敗輸出。
- root `go test ./... -count=1` 使用 repo-local cache 與 external linker flags 後超過兩分半無輸出，已中止；此狀態符合先前本機 macOS Go compile path 卡住紀錄，未產生測試失敗輸出。

## GitHub push

- 初次推送成功：`git push --force-with-lease=main:62b353889f6ec158aa682aadb74d1fb4681e6e2a origin main` 將 `main` 從遠端中繼 commit `62b353889f6ec158aa682aadb74d1fb4681e6e2a` 更新到 `31e75217a7bfed3e33ca3b072540e863b6e8881d`。
- Tag 推送成功：`git push --force origin refs/tags/v1.0.80 refs/tags/v1.0.81` 已將上一輪 `v1.0.80` tag 修正到 `f3746b9fe2e7cd332ac7a6387415e1b13f81a8d4`，並新增 `v1.0.81` tag 指向 `31e75217a7bfed3e33ca3b072540e863b6e8881d`。
- 第一次 publish evidence 寫回後，release commit amend 為 `0cb42992c1e964bb3433b02859aaab390262f1dc`，並以 `git push --force-with-lease=main:31e75217a7bfed3e33ca3b072540e863b6e8881d origin main` 成功更新遠端 `main`。
- `v1.0.81` annotated tag 已重新標記並以 `git push --force origin refs/tags/v1.0.81` 成功更新。
- 最終收尾以 `git rev-parse HEAD origin/main 'v1.0.81^{}'` 驗證 `HEAD`、`origin/main` 與 `v1.0.81^{}` 對齊為準。
