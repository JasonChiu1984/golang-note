# v1.0.80 更新紀錄

- 更新日期：2026-07-06
- 完整日期時間：2026-07-06 06:01:30 CST +0800
- 版本：v1.0.80
- 主題：Deployment controller config contract gate

## 對應 artifact

- 審查報告：`審查報告/2026-07-06-060130-資深工程師審查報告.md`
- 內容需要更新的部分：`內容需要更新的部分/2026-07-06-060130-內容需要更新的部分.md`
- 更新資料：`更新資料/2026-07-06-060130-v1.0.80-更新紀錄.md`

## 更新內容

- 新增 `scripts/check-deployment-controller-config-contract.mjs`，固定 Deployment controller config contract gate。
- 新增 `production-api-worker/Makefile` 的 `deployment-controller-config-check`。
- 新增 `.github/workflows/ci.yml` 的 `Check deployment controller config contract`。
- 同步 `README.md`、`production-api-worker/README.md`、`production-api-worker/docs/operational-runbook.md`、`production-api-worker/docs/api-contract.md`、`production-api-worker/api/openapi.yaml`、`chapters/09-build-and-deploy.md`、`chapters/11-advanced-testing.md`、`Cheatsheet/cheatsheet-advanced.md` 與 `圖解筆記3-4整合/golang-complete-visual-course.html`。
- 將 Contract gate inventory 從 47 個 root contract checker 更新為 48 個。
- 將 `圖解筆記3-4整合/golang-complete-visual-course.html` 複製並同步為 `docs/index.html`。
- 將 `VERSION`、`CHANGELOG.md`、version-sensitive contract checks、API contract 與 OpenAPI 版本標記更新為 `v1.0.80`。

## 驗證紀錄

- 官方 Go Release History 已於 2026-07-06 查核，最新支援 patch 仍為 go1.26.4 / go1.25.11，本輪不需更新 ReleaseNote patch token。
- `node --check scripts/check-deployment-controller-config-contract.mjs`：通過。
- `node scripts/check-deployment-controller-config-contract.mjs`：通過。
- `cd production-api-worker && make deployment-controller-config-check`：通過。
- `node scripts/check-contract-gate-inventory-contract.mjs`：通過，確認 48 個 root contract checker。
- `node scripts/check-openapi-contract.mjs`：通過。
- `node scripts/fix-docs-index-links.mjs --sync-source`：通過並同步 `docs/index.html`。
- `node scripts/fix-docs-index-links.mjs --check`：通過。
- `node scripts/check-html-home-links.mjs`：通過，70 個頁面可回到 `docs/index.html`。
- `node scripts/check-release-artifact-chain-contract.mjs`：通過，確認 `v1.0.80` 與 `2026-07-06-060130` artifact chain。
- 全部 48 個 root `scripts/check-*-contract.mjs`：通過。
- GitHub Actions YAML parse：通過。
- `production-api-worker/api/openapi.yaml` YAML parse：通過。
- `cd production-api-worker && docker compose config`：通過。
- `git diff --check`：通過。
- `go test ./... -count=1` 使用 repo-local `TMPDIR` / `GOCACHE` / `GOMODCACHE` / `GOFLAGS='-ldflags=-linkmode=external'` 執行超過 3 分鐘無 stdout/stderr；本機 sandbox 不允許 `ps` / `pkill` 取得 process list，因此本輪不列為通過證據，且沒有 Go test failure output。
- `cd production-api-worker && go test ./internal/config ./internal/migration -count=1` 使用 repo-local cache 執行約 60 秒無 stdout/stderr 後中止；沒有 Go test failure output。

## 發布狀態

- GitHub push：成功。
- 初次 push：`git push origin main refs/tags/v1.0.80` 成功，`main` 從 `efed8eb` 推進到 `28bed2a`，並建立新 tag `v1.0.80`。
- Final evidence commit：`455a1dc55ec3435f7cd4c622b17665aa2d031c7d`。
- Final push：`git push --force-with-lease=main:28bed2a origin main` 成功，`main` 從 `28bed2a` force update 到 `455a1dc`。
- Final tag update：`git push --force origin refs/tags/v1.0.80` 成功，`v1.0.80` tag 已 force-update 到 final evidence commit。
- Final record push：本檔將「會再建立 final record commit」修正為穩定描述後，local final record commit 與 local `v1.0.80` tag 已建立；後續 `git push --force-with-lease=main:62b3538 origin main` 連續多次因 DNS 無法解析 `github.com` 失敗。
- 狀態：remote 已發布到 `62b3538`；local final record complete，但最後一個 release-record wording cleanup 尚未推到 GitHub。待 DNS / network 恢復後補跑 `git push --force-with-lease=main:62b3538 origin main && git push --force origin refs/tags/v1.0.80`。
- `docs/index.html`：已由 `圖解筆記3-4整合/golang-complete-visual-course.html` 同步。
