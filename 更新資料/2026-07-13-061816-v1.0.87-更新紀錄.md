# v1.0.87 更新紀錄

- 更新日期：2026-07-13
- 完整日期時間：2026-07-13 06:18:16 CST +0800
- 版本：v1.0.87
- 本輪主題：Release version consistency contract gate

## 對應 artifacts

| 類型 | 檔案 |
|---|---|
| 審查報告 | `2026-07-13-061816-資深工程師審查報告.md` |
| 內容需要更新的部分 | `2026-07-13-061816-內容需要更新的部分.md` |
| 更新資料 | `2026-07-13-061816-v1.0.87-更新紀錄.md` |

## 更新內容

| 項目 | 內容 |
|---|---|
| 版本號 | 將 `VERSION` 與 current release surfaces 更新為 `v1.0.87` |
| 新增 contract | 新增 `scripts/check-release-version-consistency-contract.mjs` |
| Makefile | 新增 `release-version-consistency-check` |
| GitHub Actions | 新增 `Check release version consistency contract` |
| Contract inventory | 從 51 個 root contract checker 推進到 52 個 root contract checker |
| 文件同步 | 更新 README、production README、API contract、OpenAPI、第 7 / 9 / 11 章、進階 Cheat Sheet、整合視覺課程與 `docs/index.html` |
| 發版 artifact chain | 將本輪審查報告、內容需要更新的部分與更新紀錄納入 `scripts/check-release-artifact-chain-contract.mjs` |

## 驗證紀錄

已完成：

- `node --check scripts/check-release-version-consistency-contract.mjs`
- `node --check scripts/check-release-artifact-chain-contract.mjs`
- `node --check scripts/check-contract-gate-inventory-contract.mjs`
- `node --check scripts/check-docs-publishing-contract.mjs`
- all root `scripts/check-*-contract.mjs` sweep：52 scripts passed
- `node scripts/check-contract-gate-inventory-contract.mjs`
- `node scripts/check-docs-publishing-contract.mjs`
- `node scripts/check-release-version-consistency-contract.mjs`
- `node scripts/check-release-artifact-chain-contract.mjs`
- `node scripts/fix-docs-index-links.mjs --check`
- `node scripts/check-html-home-links.mjs`
- `cd production-api-worker && make release-version-consistency-check release-artifact-chain-check contract-gate-inventory-check docs-publishing-check`
- Ruby YAML parse：`.github/workflows/ci.yml`、`production-api-worker/api/openapi.yaml`、`configs/prometheus/prometheus.yml`、`configs/prometheus/alertmanager.yml`
- `cd production-api-worker && docker compose config`
- `cd production-api-worker && docker compose --profile monitoring config`
- root `go test ./... -count=1`
- `git diff --check`

環境限制：

- `cd production-api-worker && go test ./... -count=1` 在本機 Go toolchain / compile path 中超過 90 秒無輸出後中斷；本輪未修改 production runtime code，已以 52 個 static contract gates、Compose config、YAML parse 與 root Go tests 作為本地驗證證據。

## 發布狀態

第一次 GitHub push 成功：

```text
To https://github.com/JasonChiu1984/golang-note
   b328c96..03e87c8  main -> main
 * [new tag]         v1.0.87 -> v1.0.87
```

回填第一次 push 輸出後，已以 amend release commit、force-with-lease 更新 `main`，並 force update `v1.0.87`：

```text
To https://github.com/JasonChiu1984/golang-note
 + 03e87c8...13fd455 main -> main (forced update)
To https://github.com/JasonChiu1984/golang-note
 + 0c1f54b...1641a29 v1.0.87 -> v1.0.87 (forced update)
```

本檔最後文字收斂後，需再次推送 amended `main` 與 forced tag，並以 `git rev-parse HEAD origin/main v1.0.87^{}` 確認三者最終對齊。
