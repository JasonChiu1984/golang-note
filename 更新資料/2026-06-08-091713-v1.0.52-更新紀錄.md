# v1.0.52 更新紀錄

更新時間：2026-06-08 09:17:13 CST +0800

版本：v1.0.52

## 更新摘要

本輪依據 `審查報告/2026-06-08-091713-資深工程師審查報告.md` 與 `內容需要更新的部分/2026-06-08-091713-內容需要更新的部分.md`，將既有 Docker Compose smoke 流程升級為正式 `Compose smoke contract gate`。本輪未新增 runtime 行為，重點是固定端到端啟動驗證的文件、Makefile、CI、runbook、章節、OpenAPI 與整合視覺課程一致性。

## 修改內容

| 類別 | 內容 |
|---|---|
| Static gate | 新增 `scripts/check-compose-smoke-contract.mjs`，固定 Compose smoke 文件、script、runbook、OpenAPI、Makefile 與 CI 入口 |
| Makefile | `production-api-worker/Makefile` 新增 `compose-smoke-check` |
| CI | `.github/workflows/ci.yml` 新增 `Check Compose smoke contract` |
| API contract | `production-api-worker/docs/api-contract.md` scope 與 compatibility rules 加入 Compose smoke contract |
| OpenAPI | `production-api-worker/api/openapi.yaml` version 更新為 `v1.0.52`，description 加入 Compose smoke contract |
| 教材文件 | README、production README、第 7 / 9 / 11 章、進階 Cheat Sheet 與整合視覺課程加入 Compose smoke static gate |
| Pages 入口 | `圖解筆記3-4整合/golang-complete-visual-course.html` 已重新同步至 `docs/index.html` 並套用 GitHub Pages link fix |
| 版本紀錄 | `VERSION` 與 `CHANGELOG.md` 更新為 `v1.0.52` |

## 官方 Go 基準

本輪重新查官方 Go Release History，2026-06-08 發版評估仍採 Go 1.26.4 與 Go 1.25.11 作為 2026-06 的 patch baseline。

## 驗證紀錄

已通過：

```bash
node --check scripts/check-compose-smoke-contract.mjs
node scripts/check-compose-smoke-contract.mjs
cd production-api-worker && make compose-smoke-check
for f in scripts/check-*-contract.mjs; do node --check "$f" && node "$f"; done
node scripts/fix-docs-index-links.mjs --sync-source
node scripts/fix-docs-index-links.mjs --check
node scripts/check-html-home-links.mjs
ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'
TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1
cd production-api-worker && TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' make ci-contract
cd production-api-worker && TMPDIR=$PWD/.tmp GOCACHE=$PWD/.gocache GOMODCACHE=$PWD/.gomodcache GOFLAGS='-ldflags=-linkmode=external' go test ./... -count=1
cd production-api-worker && docker compose config
git diff --check
```

## 發布狀態

程式與文件更新已完成並通過本地驗證。已建立 release commit（`Release v1.0.52 compose smoke contract gate`）與 annotated tag `v1.0.52`，並執行：

```bash
git push origin main refs/tags/v1.0.52
```

推送結果成功：`main` 已推進到本輪 release commit，遠端新增 tag `v1.0.52`。後續因本紀錄補正而 amend 同一個 automation commit，最終遠端 `main` 與 `v1.0.52^{}` 需以最後一次 ref 對齊驗證為準。
